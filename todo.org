* Directories
** file:.
* Tasks
** TODO t0004 Test the 'ort repair' subcommand, particularly for adding identifiers to task lists without them
** TODO t0008 Make ortask.py work with .org files with tasks that don't have ids on them
** TODO [#A] t0009 Keep interactive sessions inside one bounded application
Implement the persistent 20-row inline application described in =docs/roadmap.md=.
*** TODO [#B] t0009.6 Define and test controlled final-frame and failure behavior
Retain at most one factual final frame, erase misleading failure output, and
restore the terminal after signals, exceptions, and resize failures.
**** TODO [#B] t0009.6.2 Harden signal, exception, and terminal-failure cleanup
** DONE t0011 Remove obsolete one-shot interactive selector code
Delete =_run_selector=, =select_menu=, =select_project_menu=, and their
compatibility-only tests after confirming no supported production path uses
them. Keep the persistent =InlineMenuSession= path and non-TTY numbered menus.
** DONE t0012 Refresh agent instructions for the implemented repository
Keep each agent's repository guidance aligned with the implemented scripts,
package boundaries, optional TUI dependencies, pytest suite, task-file
resolution, supported commands, and data-safety rules.
*** DONE t0012.1 Refresh AGENTS.md
*** DONE t0012.2 Have Claude refresh CLAUDE.md
*** DONE t0012.3 Have Gemini refresh GEMINI.md
** TODO t0013 Assess interactive performance and responsiveness
Measure input-to-repaint latency for navigation, view transitions, filtering,
large task files, and terminal resize. Add focused regression measurements for
repeatable bottlenecks rather than relying only on subjective timing.
*** TODO t0013.1 Eliminate the standalone Esc delay when leaving task details
Compare =Esc= with =b=/=q= to confirm prompt_toolkit's escape-sequence timeout
is responsible, then reduce the delay without breaking arrow keys, Meta keys,
or slower terminal connections.
** TODO [#A] t0015 Expose state and priority as direct context actions
Keep state and priority available on highlighted tasks in both the list and
issue workspace rather than representing them as pseudo-field rows.
*** TODO [#A] t0015.1 Add a bounded picker for supported task states
*** TODO [#A] t0015.2 Add plain-key state and priority commands
Keep Shift-arrow aligned with Org while providing discoverable alternatives
for terminals that do not reliably report those chords.
** TODO [#A] t0016 Build the full orti issue-editing workspace
Turn =ortask.py -i= into the compact Org-backed issue editor described in
=docs/roadmap.md=, with the external editor retained as a secondary escape
hatch.
*** TODO [#A] t0016.4 Add a hierarchical subtask mini-list
Show all descendants in Org source order with indentation, independent
highlight and scrolling, direct actions, and child-workspace navigation.
**** TODO [#A] t0016.4.3 Add direct subtask state and priority actions
Apply task-list-style triage commands to the highlighted viewport task.
*** TODO [#B] t0016.5 Add direct tag and standard metadata editing
Support useful Org-native issue metadata without inventing a private schema.
*** TODO [#B] t0016.6 Add workspace terminal, safety, and performance coverage
Cover long bodies and subtask lists, narrow and resized terminals, focus,
Help, cancellation, save/discard/recovery, and PTY lifecycle behavior.
** TODO [#A] t0018 Make the orti viewport adaptive and optionally full-screen
Fit short views to their content, grow richer views to the normal 20-row
inline ceiling, and support deliberate fixed-height and full-screen modes as
specified in =docs/roadmap.md=.
*** TODO [#A] t0018.1 Extract and test a view-driven viewport policy
Separate minimum/preferred body rows, requested auto or numeric height, chrome,
terminal reserve, and effective height without adding task-specific rules to
=InlineMenuSession=.
*** TODO [#A] t0018.2 Fit compact lists and resize with visible content
Use compact chrome so a three-task list can occupy about seven rows; grow when
a subtree expands or a larger view opens, and safely contract after collapse
or Back without leaving stale terminal rows.
*** TODO [#A] t0018.3 Give each view explicit sizing preferences
Define useful minimum and preferred body heights for task/project lists, Help,
pickers, confirmations, text input, and the planned issue workspace.
*** TODO [#B] t0018.4 Add Handrail viewport scrolling and Resize mode
Support =--height auto= and =--height N= with terminal clamping. Use
=Alt-Up=/=Alt-Down= to scroll the active viewport, and =C-^=/=C-6= to enter a
mode with Up/Down height adjustment, =a= auto-fit, and =m= inline maximize.
*** TODO [#B] t0018.5 Add an explicit full-screen presentation mode
Support =--full-screen= with the same views and commands in the alternate
screen. Add =f= to Resize mode only after the runtime transition can preserve
stack, focus, buffered edits, and terminal state; no function-key binding is
required.
*** TODO [#A] t0018.6 Cover adaptive and full-screen terminal behavior
Add pure sizing tests, pipe-input view-transition tests, and PTY coverage for
growth, contraction, =C-^= input, Alt-arrow viewport scrolling, stale-line
cleanup, inline final frames, and full-screen alternate-screen restoration.

** DONE [#A] t0019 Make the project layer worth opening
Implement the Project Navigator section of =docs/roadmap.md= against the model
in =docs/projects.md=: explicit project membership, frictionless registration,
and one project list shared by =pmgr=, =ptui=, and =pcd=.
*** DONE [#A] t0019.1 Mark projects by symlink and make task files optional
Replace =manager.SKIP_PROJECT_DIRS= with the outward-symlink marker, list
projects that have no task file, and mark broken project symlinks instead of
omitting them.
*** DONE [#A] t0019.2 Add a one-word project registration verb
Add =add= alongside =projadd=: with no argument, walk upward to the project
root and print what was registered; take an explicit path literally.
*** DONE [#B] t0019.3 Rename orgmgr.py to projmgr.py and move pcd with it
Update =misc/ortask-completion.bash=, the =pmgr=/=ptui= aliases, and rename
=docs/orgmgr.md= to =docs/projmgr.md=.
*** DONE [#B] t0019.4 Move projtui.py into ortasklib and delete the script
Task UI to =ortasklib=, project browser to =projmgr.py=. Sequence after
=t0011= rather than racing it.
*** DONE [#B] t0019.5 Converge the pcd picker and the project list
One project view with =--out= deciding what Enter does, removing the second
copy of project rendering.
*** DONE [#C] t0019.6 Rename migrate to init and add rm and doctor
=migrate= has not migrated anything since =projtui.ini= was removed.
** TODO t0020 Route the remaining parsing through orglib
Sixteen =core.parse_org()= call sites still name =core=; only
=manager.summarize_projects= and =projmgr._project_tasks= go through
=orglib.parse()=. Move the read paths over, switch callers to
=orglib.TodoItem=, and drop the re-export block in =ortasklib/core.py= once
nothing depends on it. Mutation stays behind until a caller needs it.
** TODO t0021 Add optional orgparse conformance tests
Independent validation that files ortask writes are really Org, per
=docs/orglib.md=. Use =pytest.importorskip= so the suite still runs with
nothing installed, plus an =ORTASK_REQUIRE_ORGPARSE=1= switch that turns the
skip into a failure for one CI job — otherwise the test rots unrun. Sequence
after =t0023=, or configure =TODO= as active and
=DONE=/=MOOT=/=SUPERSEDED= as terminal with =env.add_todo_keys=.
** TODO t0022 Try orgmunge as a test-only differential reader
Use =pytest.importorskip= and assert orgmunge and =orglib= report the same tasks
across a checked-in, hermetic corpus. A separate opt-in probe may inspect the
live registry; normal tests must not depend on it. Reads only: a wrong read
costs a listing, a wrong write costs a file. Measured 2026-08-21: orgmunge
normalizes keyword case, tag padding, and blank lines before top-level headings,
and cannot parse either =llm-log.org= (inactive timestamp in the heading).
Record expected incompatibilities explicitly rather than silently omitting them.
After =t0028=, compare whole-document parsing with context-enveloped =* Tasks=
regions so the broker's extraction and context rules are tested too.
** TODO t0023 Declare task keywords in-file with #+TODO:
Add =#+TODO: TODO | DONE MOOT SUPERSEDED= to task files. Keywords to the right
of the bar are terminal states, matching =orglib.TERMINAL_STATES=. Without a
declaration, orgparse, orgmunge, and Emacs read =** MOOT t0007 x= as an ordinary
heading whose text starts with "MOOT". Decide whether =ort= writes the line on
new files and whether =repair= offers to add or reconcile it.
** TODO t0024 Choose a license for the suite
There is no =LICENSE= file, which blocks judging any dependency's license
against ours — =orgformat= is GPL-3, while =orgparse= (BSD-2) and =orgmunge=
(MIT) are permissive. Resolve this before publishing the suite or adding a hard
runtime dependency; it does not block optional, test-only evaluation of
permissively licensed libraries.
** TODO t0025 Enforce the *-private.org reservation in task-file discovery
=docs/projects.md= says private files are never candidates for task-file
resolution, but =manager.choose_org_file= excludes only the literal name
=directories-private.org=. Verified 2026-08-20: a =notes-private.org= in a
registry entry is selected as that project's task file. Match the suffix.
** DONE t0026 Implement and migrate to the registry index file
Replace per-entry =directories-private.org= files with one =projects.org= at
the registry root, per =docs/config.md=. The index's existence is the migration
marker: private-stack consumers do not fall back to the old files. This task is
the gate for =t0031=. The =migrate= name is reclaimed here for an actual
registry-data migration; it no longer remains an alias for =init= after the
cutover.
*** DONE t0026.1 Add source-backed registry index parsing to orglib
Find the unique top-level heading matching a project and only its direct child
=** Directories= section. Preserve source spans and distinguish a missing
project, a missing section, and an empty section; reject duplicate project or
direct-child =Directories= headings. Reach this through =orglib.parse(text)=,
not =core=. This incrementally completes the directory-parsing slice of
=t0020= without waiting for every parse call site to move. Test direct-child
selection, ignored deeper headings, missing-versus-empty results, duplicate
errors, and exact source spans.
*** DONE t0026.2 Implement pmgr migrate
Reclaim =migrate= from its deprecated =init= alias. =pmgr migrate [--dry-run]=
validates every legacy private file before writing, creates =projects.org=
atomically, then removes the migrated files. Preserve each accepted legacy
document by nesting its contents under the matching project heading and
demoting headings one level; reject constructs that cannot be nested safely
rather than discard them. Create an empty index when no private files exist so
the registry is still marked migrated. If cleanup is interrupted, a later run
may remove a legacy file only when it matches its index section; disagreement
is a hard error requiring manual resolution. Fixture tests cover dry-run with
no mutation, whole-file validation before writing, preservation of accepted
content, successful cleanup, interrupted cleanup, and conflict refusal.
*** DONE t0026.3 Cut private-stack consumers over to projects.org
Read private stacks only from =projects.org=. Make =cdproj= reject missing or
incomplete migration, and make =doctor= diagnose index problems. Keep =list=,
=add=, =rm=, and =init= available before migration. Only =migrate= may read
legacy private files.
** TODO t0027 Make orglib independently testable and consumable
Move its contract tests into a focused module that imports only =orglib= and
can run without importing =ortasklib=. Define how another repository such as
castabout installs and versions it before promising =orglib= as a standalone
dependency; add packaging metadata only when that consumer path is chosen.
** TODO t0028 Define source-backed regions and separate task policy
=orglib/syntax.py= currently combines heading parsing with ortask's state names,
ID grammar, and =Tasks=/=Template=/=Directories= conventions. Define a generic
=Region= with an original source span, text, heading depth, context envelope,
and source revision. Decide which task conventions belong in a configurable
dialect or adapter while keeping region detection and source mapping generic.
** TODO t0029 Define region-edit conformance before adding writers
Create before/after fixtures for state, priority, title, body, add, template,
and archive operations. Test exact relative patches separately from explicitly
normalizing whole-region replacements. Assert the semantic result, reject stale
or out-of-bounds edits, and preserve everything outside the declared ranges.
Use the same matrix for the bespoke writer, orgmunge, direct Tree-sitter, and
org-rw approaches; an unchanged =render()= test alone is not sufficient.
** TODO t0030 Prototype brokered partial-document editing
After =t0028= and =t0029=, extract =* Tasks= as a source-backed region, supply
file keywords and inherited context in a synthetic standalone document, and
map backend output back to the original span. Exercise orgmunge as a test-only
adapter first. Report normalization inside the region and prove the rest of the
file remains exact; do not expose a production writer or runtime selector yet.
** DONE t0031 Save shell directory stack to private dirstack (cdproj -s / pmgr set-dirs)
Implement =projmgr.py set-dirs [--project PROJECT]= and =cdproj -s [PROJECT]=
to write the live shell directory stack (=dirs -l -p=) into the project's
private directory list. After all of =t0026= that list is the project's section
in =<registry>/projects.org=, so this is the first verb to edit a shared Org
file in place rather than create one. Specification: "Saving the directory
stack" in =docs/cdproj.md=.
*** DONE t0031.1 Implement pmgr set-dirs
Current-project detection from =$PWD= (an error rather than a guess when no
project or two projects match), explicit =--project= override, positional
directories plus =--stdin=, =~= for paths under =$HOME=, dedupe by resolved
path, additions written without asking, subtractions prompted
=[r]emove=/=[k]eep=/=[c]ancel= with =--missing= as the non-interactive answer,
and =--dry-run=. Zero directories, including empty =--stdin=, is an error;
clearing a list needs a future explicit operation. Require a fully migrated
index and test that every byte outside the rewritten =Directories= subtree is
unchanged.
*** DONE t0031.2 Add the cdproj -s shell half
Teach =misc/cdproj.func.sh= to recognize =-s=/=--save= and pass =dirs -l -p= as
arguments rather than on stdin, so the subtraction prompt still has a terminal,
mapping its optional project argument to =set-dirs --project=. Complete project
names after =-s= in =misc/ortask-completion.bash=. The =--out= protocol is
untouched: this path writes no stack file and does not =cd=.
** TODO t0032 Settle the repair/doctor naming and whether both should exist
=ort repair= and =pmgr doctor= are the same kind of command — check a
thing, report what is wrong — on two different subjects, under two
unrelated names, and =repair= does not repair anything
(=ortask.py:241=; its subparser help still claims "find and fix ID
problems"). Gemini's assessment in =docs/llm-assessments.md=
recommends renaming =doctor= to =check= or =audit=.  Decide one naming
scheme for both, whether the pair collapses into one verb with a
subject, and what =repair= promises before =t0004= and =t0008= build
on it. Blocks the question in =docs/logging.md= of whether their
findings are logged.
** DONE t0033 Implement the ortask event log
The event log specified in =docs/logging.md=: an append-only JSON Lines record
of what the tools did, at =<registry>/log/YYYY-MM.jsonl= beside =projects.org=,
read back through =ort log=. It is derived and disposable — nothing may read it
to decide a task's state. Two follow-ups are deliberately not subtasks here:
per-project =:LOG:= control waits for the =projects.org= property drawer in
=docs/ptui.md=, and merging Org's own =CLOSED:=/=:LOGBOOK:= timestamps so that
edits made in Emacs are visible is a separate decision.
*** DONE t0033.1 Add ortasklib/log.py and the append-only writer
New =ortasklib/log.py= exposing =record(event)= and a settable sink for tests,
and nothing else. The path is =<registry>/log/YYYY-MM.jsonl= derived from
=manager.resolve_registry()=; =[log] enabled= in =ortask.ini= gates it and an
absent section means off; =ORTASK_LOG= (=on=/=off=) and =ORTASK_LOG_DIR=
override the file. Write with =O_APPEND=, one =write()= per event, capped at
4096 bytes so concurrent invocations cannot interleave, truncating the title
with an explicit marker rather than dropping the event. Never raise into the
caller and never create the registry. Tests: off by default, on via ini and via
env, the field set from =docs/logging.md=, truncation at the cap, a missing
registry logs nothing, a write failure leaves the exit code alone, and two
processes appending concurrently produce two intact lines.
*** DONE t0033.2 Emit events from the task and project write paths
Call =log.record()= from the points that commit a change: =cmd_add=,
=cmd_done=, =cmd_open=, =cmd_archive=, =cmd_apply=, and =cmd_init= in
=ortask.py=; =cmd_add=, =cmd_rm=, =cmd_set_dirs=, and =cmd_migrate= in
=projmgr.py=; and the save paths in =taskui.py=. One event per task affected,
sharing one =session= per invocation. Not from =ortasklib/tasks.py=: those
helpers take text and return lines, and a buffered TUI edit that is discarded
must not have logged. Tests: one event per verb carrying the documented fields,
=archive= of three tasks emitting three events under one =session=,
=list=/=show=/=doctor=/=repair= emitting nothing, and a discarded TUI edit
emitting nothing.
*** DONE t0033.3 Log cdproj as a deliberate project switch
=pmgr cdproj= records the switch and the resolved stack; opening =ptui= and
scrolling records nothing. The test is deliberate act versus looking around
(=docs/logging.md=). All three routes into a stack — the picker, =cdproj
PROJECT=, and the numbered fallback — already share =write_selection()=, so one
call site covers them. Tests: each route logs exactly one event, cancelling the
picker logs nothing, and browsing the navigator logs nothing.
*** DONE t0033.4 Add the ort log and pmgr log readers
=ort log= and =pmgr log=, registered alphabetically across both parsers,
dispatch tables, help, and =misc/ortask-completion.bash=. Flags per
=docs/logging.md=: =--since=, =--until=, =--all= (=ort=), =--project= (=pmgr=),
=--limit=, =--day-start HH:MM=, and =--format plain|json|org=. =ort log=
defaults to the resolved file's project and =pmgr log= to every project. =WHEN=
accepts =today=, =yesterday=, =week=, =NNd=/=NNh=, =YYYY-MM-DD=, or an ISO
timestamp. No output is exit 0, and so is a missing log directory. =--format
json= must be a byte-identical passthrough of the matching lines, since that is
the extension contract; no =zim= format belongs here. Tests: each =WHEN= form,
the day boundary with and without =--day-start=, scoping for both tools,
passthrough fidelity, and a range spanning two monthly files.
*** DONE t0033.5 Reserve log/ in the registry and document the configuration
=pmgr doctor= reports any unrecognized registry subdirectory as a note (=docs:
not a project entry, ignored=); teach it that =log/= is expected and say
nothing. Add =log/= to the reserved names in =docs/config.md= beside
=projects.org=, record the =[log]= section there as the deliberate exception to
the one-machine-global-setting rule, and add =ORTASK_LOG= and =ORTASK_LOG_DIR=
to the environment table. Mention the =log/= gitignore choice for a registry
under version control. Tests: =doctor= stays silent about =log/= and still
reports other stray directories.
** TODO t0034 Prototype the zim journal extension
=ortask-zim=, a pull extension per =docs/extensions.md=: read =ort log --format
json --since today=, group events by project, resolve each project to a zim
page, and add one tagged bullet per project under the day section of the
journal page. Read granularity and layout from the notebook's =notebook.zim=
rather than assuming daily pages, and never touch a line that lacks the
extension's own tag. Ship =--dry-run= first and prefer the attended
=zim --plugin quicknote= path before writing page files directly. Needs
=t0033.4=.
** TODO [#A] t0035 Make ptui a priority-driven project dashboard
Implement =docs/ptui.md= so the project browser helps select higher-priority
work while preserving registry symlinks as the authority for membership and
task-file resolution.
*** DONE [#A] t0035.1 Add source-backed project metadata parsing
Parse optional Org priority cookies plus =DESCRIPTION= and =TASK_FILE=
properties from each =projects.org= project section. Preserve exact source
spans, unknown properties, prose, tags, and child sections; reject ambiguous
project headings or property drawers. Needs =t0036= first: until the matcher
strips the cookie, a section this parser reads is a section
=parse_project_directories= no longer finds. Document the drawer in
=docs/config.md='s index shape notes, which currently describe only
=** Directories= and say everything else under a project heading is free text.
*** DONE [#A] t0035.2 Add priority-first project rows and sort controls
Show priority, description, and open-task count. Default to
=priority -> alphabetical= order, let =s= switch between priority and
alphabetical views, and keep the highlight anchored by project name. =s= cycles
the modes that exist so far; =t0035.5= adds Modified, which =docs/ptui.md=
lists as the third. Contextual Help lists the modes actually available, so it
stays true at each step.
*** DONE [#A] t0035.3 Add buffered project-priority editing
Use Shift-Up/Down to change priority through =unset -> C -> B -> A=. Route
changes through one bounded =projects.org= transaction with visible dirty
state, undo/redo, =C-s= save, and Save/Discard/Cancel on exit.

Decided 2026-08-22 (see "Write path" in =docs/ptui.md=): =ptui= holds one
=taskui.OrgBuffer= over =projects.org= open for the session, which is what
makes undo across several edits work, and =save()= gains the preimage check
that =manager.apply_registry_directories_update= already does. At this stage a
changed file refused save without discarding either version; =t0037= later
added in-memory reconciliation while retaining that final safety check. The
auto-save sibling =#projects.org#= stays and is reserved in =docs/config.md=.
*** TODO [#B] t0035.4 Build the project metadata workspace
Make =m= open a compact editor for priority and description while showing the
read-only project name, resolved task file, recorded task-file mirror, and
directory-stack controls. Require an already migrated registry index. Decided
2026-08-22: the workspace edits the directory stack, not only displays it, so
it and =cdproj='s =e= — which opens the index in =$VISUAL= at the project's
section (=docs/cdproj.md=) — are two entrances to one bounded region and must
share =t0035.3='s transaction path, external editor included. Two independent
writers for one section is the outcome to avoid. Build on =t0038.1='s
=taskui.OrgBuffer= adapter; do not create transaction or source-patching state
inside the metadata view.
*** TODO [#B] t0035.5 Add modified sorting and task-file mirror diagnostics
Add newest-task-file-first sorting with unreadable or absent files last.
Display disagreements between the non-normative =TASK_FILE= mirror and the
resolved task file, with an explicit refresh action.
** DONE [#A] t0036 Strip the priority cookie when matching project headings
=orglib.syntax._project_title= strips trailing tags only, so =* [#A] ortask=
does not match registry entry =ortask=: =orglib.parse(text).directories('ortask')=
reports =project_found: False=. Since =t0026.3= made the index the only private
stack source, a heading that gains a cookie silently loses its directory stack
and =cdproj= falls back to the project root. Verified 2026-08-22.
=docs/ptui.md= already tells readers both heading forms are recognized, so this
is reachable by hand-editing today, before any of =t0035= lands. Strip a leading
=[#A]=-style cookie where tags are stripped, and correct the matching rule in
=docs/config.md=, which documents tags only. Tests: =cdproj= resolves the same
stack with and without a cookie, and =set-dirs= and =migrate= preserve a cookie
and a property drawer on a section they rewrite. Blocks =t0035.1= and =t0035.3=.
** DONE t0037 Handle concurrent edits to projects.org in ptui
=t0035.3= makes a stale save refuse. Refusing is correct and unhelpful: two
people — or one person in two terminals — editing different project sections of
one file is not a conflict in any useful sense, and =pmgr set-dirs=,
=pmgr migrate=, =cdproj='s external editor, and Emacs can all touch the index
while =ptui= holds it. The goal is that the session absorbs those writes and
says so, and only refuses what it genuinely cannot reconcile. Implement after
=t0035.1= supplies project spans and =t0035.3= supplies the preimage-safe
=OrgBuffer= save path. Integrate only =ptui=/=projects.org= here, but keep the
buffer seam reusable for a later =orti= task-file reconciler. The complete
state and merge contract is in =docs/ptui.md=.
*** DONE t0037.1 Detect external changes without weakening save safety
Give =OrgBuffer= a remembered disk signature for inexpensive UI polling. Use
device/inode, size, and nanosecond mtime only as a change hint; after a hint,
read the file and compare exact text with the saved baseline. Always repeat the
exact comparison at =C-s= even when polling saw nothing. A clean buffer adopts
changed disk text as its new clean baseline. A dirty buffer reports a structured
external-change state for reconciliation. Missing or unreadable files block
save. Poll from the existing prompt-toolkit refresh path without a watcher
thread. Do not offer routine force-overwrite: the alert permits reload/discard,
continue editing, and retry. Tests cover changed, touched-but-identical,
replaced, deleted, and unreadable files plus a same-stat save-time change.
*** DONE t0037.2 Add a pure project-section three-way merge planner
Add a no-I/O =ortasklib.manager= helper taking Base (last disk text), Ours
(buffer text), and Theirs (current disk text). Use =t0035.1='s unique normalized
project names and exact source spans. Build on Theirs so external preamble,
ordering, additions, deletions, and untouched sections survive. Replay each
locally changed existing section only when Theirs still equals Base; accept an
identical Ours/Theirs result; report a structured conflict when both changed
one section differently, when a locally changed section was deleted, or when a
local edit falls outside an addressable project section. Apply replacements
back-to-front and validate the merged document. Tests cover disjoint edits,
identical edits, same-section conflicts, external add/delete/reorder, source
fidelity, malformed input, and a guarantee that the helper performs no writes.
*** DONE t0037.3 Reconcile the open ptui buffer and expose conflicts
On detection and immediately before =C-s=, call the planner in memory. A
successful dirty merge makes Theirs the new saved baseline and the merged text
the dirty buffer; collapse local undo to one rebased transaction and clear redo
so undo/discard preserve the absorbed external edit. Refresh =#projects.org#=
only afterward as ordinary crash recovery; it is never a merge input or staging
file. Leave the real index, buffer, history, and auto-save unchanged on
conflict, show a persistent alert naming the project sections, and refuse
=C-s= until reload, undo/edit plus retry, or another successful reconciliation.
After reconciliation, =C-s= performs one final exact preimage check and the
existing atomic write. Event logging compares the rebased disk baseline with
the saved merge so it does not attribute external edits to =ptui=. Tests cover
clean adoption, dirty merge, undo, discard, conflict recovery, a second change
before save, auto-save contents, event attribution, and no =projects.org= write
before explicit save.
** TODO [#B] t0038 Extract reusable reconciliation layers
Use =t0037= as the concrete extraction case rather than adding more generic
buffer and merge behavior to =ortasklib.taskui= or =ortasklib.manager=. Keep
the dependency direction explicit: a format-neutral file buffer may consume a
pure merge result; =orglib= knows Org text and source regions but no filesystem
or TUI; project-index policy maps generic conflicts to registry concepts.
This complements =t0020=: that task routes existing parsing calls, while this
one prevents new parsing and reconciliation machinery from accumulating in the
old package.
*** DONE [#B] t0038.1 Isolate the format-neutral file buffer
Move =BufferTransaction=, =DiskSignature=, =ExternalFileChange=,
=BufferChangedError=, exact-preimage checking, undo/redo, and rebasing out of
=taskui.py= into a text-file buffer module. Make atomic writing, auto-save
naming, and post-save notification explicit hooks so the core has no Org,
event-log, project, menu, or =prompt_toolkit= dependency. Preserve every
=OrgBuffer= safety and recovery test; keep a compatibility adapter while
callers move. Start inside this repository rather than publishing a package.
*** TODO [#B] t0038.2 Generalize keyed Org-region merging
After =t0028= defines source-backed regions and =t0029= defines conformance,
extract the no-I/O keyed-region mechanics from
=manager.plan_project_index_merge()= into =orglib=. Define backend-neutral
region merge plans, exact replacements, and structured overlap/structure
conflicts. Keep Base/Ours/Theirs text inputs, back-to-front application, and
merged-document validation. Do not move registry heading names, reserved
sections, or permitted project edits into the generic engine.
*** TODO [#C] t0038.3 Thin the project-index reconciliation adapter
Give project-index policy a focused module that selects project regions,
classifies allowed local changes, maps generic conflicts to project names, and
supplies =ptui= messages. Leave =projmgr._ProjectBrowser= responsible only for
poll timing and Reload/Retry/Continue view transitions. Do not put parser or
patch logic in the browser while =t0035.4= adds metadata editing.
*** TODO [#C] t0038.4 Evaluate a standalone Emacs-style buffer library
Only after =t0038.1= has a second credible consumer, compare keeping the module
in-tree with publishing a small library for transactional text buffers,
external-change detection, auto-save, exact-preimage saves, and rebasing.
Require no Org or =prompt_toolkit= dependency and document how save observers
and merge callbacks integrate. A single ortask consumer is not enough reason
to create or version another package.
