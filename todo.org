* Tasks
** DONE t0001 Remove AI slop from docs/taskwarrior.md
** DONE t0002 create .org file if none exist in directory when using 'ort add' (calling it "tasks.org")
** DONE t0003 Make it so that there doesn't have to be a 'Tasks' section in a .org file to use it
** TODO t0004 Test the 'ort repair' subcommand, particularly for adding identifiers to task lists without them
** DONE t0005 Let's make it so that 'ort add' doesn't arbitrarily add a '* Tasks' section to anything other than maybe a new todo.org
** DONE t0006 Implement in-memory saving, with writing happening on exit (and writing only happening after confirmation)
** DONE t0007 When in menu.select_menu app mode, don't reorder the tasks based on TODO/DONE status, since that can make it so that a different task is selected upon hiting "t"
** TODO t0008 Make ortask.py work with .org files with tasks that don't have ids on them
** TODO [#A] t0009 Keep interactive sessions inside one bounded application
Implement the persistent 20-row inline application described in =docs/roadmap.md=.
*** DONE [#A] t0009.1 Add regression coverage for bounded repainting and terminal cleanup
Use pipe-input and PTY tests to cover context changes, long lists, final exit,
resize, and the absence of alternate-screen switching.
*** DONE [#A] t0009.2 Add one persistent 20-row prompt_toolkit application shell
Give the session a dynamic header, scrolling body, fixed command footer, and
terminal-aware effective height.
*** DONE [#A] t0009.3 Move project, task, detail, Help, and picker contexts onto a view stack
Preserve stable selection and scroll state when pushing and popping views.
*** DONE [#A] t0009.4 Move recovery and save/discard prompts into the bounded application
Show confirmations and outcomes in the active region without appending output.
**** DONE [#A] t0009.4.1 Move save/discard confirmation into the bounded task session
**** DONE [#A] t0009.4.2 Move recovery choices into the bounded task session
*** DONE [#A] t0009.5 Suspend and resume the session around the external editor
Use prompt_toolkit terminal handoff, then reload data and repaint the same view.
*** TODO [#B] t0009.6 Define and test controlled final-frame and failure behavior
Retain at most one factual final frame, erase misleading failure output, and
restore the terminal after signals, exceptions, and resize failures.
**** DONE [#B] t0009.6.1 Define and test controlled normal-exit retention
**** TODO [#B] t0009.6.2 Harden signal, exception, and terminal-failure cleanup
** TODO t0010 Archive completed tasks into a separate org file
Implement the =todo.org_archive= archiving behavior described in =docs/roadmap.md=.
** TODO t0011 Remove obsolete one-shot interactive selector code
Delete =_run_selector=, =select_menu=, =select_project_menu=, and their
compatibility-only tests after confirming no supported production path uses
them. Keep the persistent =InlineMenuSession= path and non-TTY numbered menus.
** TODO t0012 Refresh agent instructions for the implemented repository
Update =AGENTS.md= and =CLAUDE.md= to describe the current scripts,
=ortasklib/= package, prompt_toolkit dependency, pytest suite, task-file
resolution, and supported commands; check =GEMINI.md= for the same drift.
** TODO t0013 Assess interactive performance and responsiveness
Measure input-to-repaint latency for navigation, view transitions, filtering,
large task files, and terminal resize. Add focused regression measurements for
repeatable bottlenecks rather than relying only on subjective timing.
*** TODO t0013.1 Eliminate the standalone Esc delay when leaving task details
Compare =Esc= with =b=/=q= to confirm prompt_toolkit's escape-sequence timeout
is responsible, then reduce the delay without breaking arrow keys, Meta keys,
or slower terminal connections.
** DONE t0014 Edit task text from within the bounded session
Add a heading-text edit path as described in =docs/roadmap.md=: a
=tasks.py= text-rewrite helper plus a focused text-entry view.
*** DONE t0014.1 Add a surgical heading-text rewrite helper
Preserve the rest of the Org heading and reject text that would alter its
structure when reparsed.
*** DONE t0014.2 Add focused text entry to the bounded task-detail workflow
** TODO t0015 Complete state and priority editing in the list view
Priority editing already works. Add the bounded state picker and plain-key
bindings described in =docs/roadmap.md=.
