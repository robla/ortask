* robla 2026-08-20 - I want the projmgr.py (cdproj/ptui) project-level
  configuration to be literate.  I like using symlinks as pointers to
  directories (because that is what they are), but also I think the
  current project configuration is going down the right path.  It
  seems like having maybe one (but maybe many) .org file in the
  directory pointed to in ~/.config/ortask/ortask.ini is the right way
  to go.  Not sure which way to go, though.