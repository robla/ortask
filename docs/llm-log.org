#+TITLE: ortask LLM log

* About this log
A running, retroactive work log for =ortask=, reconstructed from git
history and the current docs. Each entry is a single line:

: ** Name [YYYY-MM-DD Ddd HH:MM]: short description of the substantive change

Conventions:
- One entry per substantive repository edit.
- Speakers are the assisting models: ChatGPT, Codex, Gemini, Claude.
- This file is a best-effort reconstruction from commits and diff
  messages, not a claim that the models self-authored these notes at the
  time.

* Bootstrap note
This log was reconstructed after the fact so the project has a compact
LLM-oriented history in one place. The entries below follow the visible
repo evolution rather than trying to capture every prompt or revision.

* LLM log
** ChatGPT [2026-04-08 Wed 21:36]: drafted the initial repo scaffold around an Org-mode task database, including =README.org=, =ortask.py=, =docs/ortask.md=, and the first design notes.
** ChatGPT [2026-04-08 Wed 22:11]: added the first round of LLM-oriented repo metadata, including =AGENTS.md= and model-specific guidance files.
** Claude [2026-04-08 Wed 23:03]: updated =ortask.py= to more closely match the negotiated Org TODO design, and synchronized the supporting docs.
** ChatGPT [2026-04-08 Wed 23:43]: refined the adjacent-trackers comparison and reordered the list to better reflect the alternatives being considered.
** Claude [2026-04-08 Wed 23:19]: tightened the default listing behavior so only TODO items show by default and simplified the CLI options.
** Claude [2026-04-08 Wed 23:58]: switched task IDs to lowercase =t0001=-style identifiers across docs, README, and code.
** Claude [2026-04-09 Thu 00:03]: made =ortask.py help= a synonym for =ortask.py --help=.
** Claude [2026-04-09 Thu 01:01]: changed the default file search order to prefer =todo.org= and =tasks.org= before falling back to other =*.org= files.
** Gemini [2026-04-09 Thu 01:32]: added =docs/taskwarrior.md= to compare Taskwarrior workflows with =ortask=.
** Claude [2026-04-09 Thu 01:36]: improved the output produced by =ortask.py add= and added a follow-up TODO in =README.org=.
** Claude [2026-04-09 Thu 01:42]: split the task list out of =README.org= into =todo.org=.
** Claude [2026-04-09 Thu 01:56]: added the first follow-up tasks to =todo.org=.
** Claude [2026-04-11 Sat 15:34]: revised =docs/ortask.md= to keep the command reference aligned with the current CLI and default file lookup behavior.
** Codex [2026-06-11 Thu 23:51]: created =docs/interactive.md= specifying a future interactive/work mode for stepping through =todo.org= tasks, including actions, authoring conventions, toolkit direction, safety rules, and tests.
** Codex [2026-06-11 Thu 23:51]: updated =AGENTS.md= and this log's conventions so future user-requested repository edits include a concise =docs/llm-log.org= entry.
** Codex [2026-06-12 Fri 12:07]: revised =docs/interactive.md= around a project-selection TUI and task menu workflow for =projtui.py=.
** Codex [2026-06-12 Fri 12:52]: added a stdlib-only =projtui.py= project menu helper and documented the minimal Electorama Weekly promotion workflow in =docs/interactive.md=.
** Codex [2026-06-12 Fri 13:11]: added global =projtui.py= project-directory configuration, documented =--projdir= and the config file, and installed RobLa's config pointing to =~/tmpsorta/proj2026=.
** Codex [2026-06-12 Fri 14:45]: created =docs/format.md= as a minimal Org format stub for ortask task files, using Electorama Weekly chores as the concrete example and documenting TODO file discovery direction.
** Codex [2026-06-12 Fri 14:57]: reformatted =~/tmpsorta/electorama-weekly/TODO-ElWeek.org= into an ortask-compatible weekly promo checklist with a preserved destination template.
** Codex [2026-06-12 Fri 15:17]: added support for weekly task IDs like =tw26W24=, updated the Electorama Weekly checklist to use week IDs, and documented the convention in =docs/format.md= and =docs/ortask.md=.
** Codex [2026-06-12 Fri 15:59]: updated the Electorama Weekly template to mirror the handcrafted weekly checklist and documented bare task links as body lines so they appear in task details.
** Codex [2026-06-12 Fri 18:38]: changed =projtui.py= task selection to show details immediately and expanded editor launch support for opening selected tasks at their line number.
** Codex [2026-06-12 Fri 18:46]: updated =projtui.py= task details to include descendant subtasks and cap displayed detail text at 20 lines.
** Codex [2026-06-12 Fri 18:49]: added subtask drill-down menus to =projtui.py= so selected tasks offer direct subtasks as numbered items to view.
** Codex [2026-06-12 Fri 19:20]: created =docs/scan.md= specifying a read-only =ortask.py scan= command that reuses =projtui.py= project discovery and reports top-level tasks per project.
** Codex [2026-06-12 Fri 19:30]: rewrote the scan spec as =docs/orgmgr.md=, defining =orgmgr.py list= as the global project/task overview while keeping =ortask.py= scoped to local Org task files.
** Gemini [2026-06-12 Fri 19:43]: updated =GEMINI.md= to align with the current implementation and documentation in =docs/= (including lowercase IDs, weekly tasks, projtui.py, and orgmgr.py specs).
** Gemini [2026-06-12 Fri 19:47]: implemented the first version of =orgmgr.py= with the read-only =list= command to scan workspaces, display projects, and list top-level tasks.
** Claude [2026-06-12 Fri 20:46]: rewrote =CLAUDE.md= to match the current docs and implementation, covering =orgmgr.py= and =projtui.py=, weekly task IDs, the corrected default-file resolution order, and the =docs/llm-log.org= convention.
** Claude [2026-06-12 Fri 21:02]: specified the =orgmgr.py projadd= subcommand in =docs/orgmgr.md=, reusing =ortask.py= task-file discovery to register projects in a new shared =~/.config/ortask/ortask.ini= =[projects]= registry, and documented the deprecation plan for =projtui.py='s =projdir= config.
** Claude [2026-06-12 Fri 21:18]: added an =orgmgr.py migrate= subcommand to =docs/orgmgr.md= that initializes the shared registry (importing any =projdir= projects) and gated =projadd= behind it with a friendly error until migration is run.
** Claude [2026-06-12 Fri 21:30]: clarified in =docs/orgmgr.md= that =orgmgr.py projadd= registers exactly one directory (cwd or the given path) and never scans subdirectories, resolving its task file there with =TODO.org=-first discovery like =ortask.py=.
** Codex [2026-06-12 Fri 21:43]: added =docs/architecture.md= describing the planned shared =ortasklib/= package split into =core.py=, =tasks.py=, and =manager.py= while keeping the scripts as thin UI/CLI layers.
** Codex [2026-06-12 Fri 21:47]: added =docs/testing.md= with nine refactor-protection tests covering parsing, ID normalization, file discovery, local edits, manager summaries, and CLI smoke checks.
** Codex [2026-06-12 Fri 21:50]: implemented the nine pytest cases from =docs/testing.md=, updated default Org file discovery to prefer =TODO*.org= before lowercase compatibility files, and refreshed the matching README/CLI reference text.
** Claude [2026-06-12 Fri 22:02]: added two pytest cases beyond the original nine, covering JSON/org list formatters (nesting + org round-trip) and =show= descendant expansion with shorthand week-ID lookup.
** Claude [2026-06-12 Fri 22:20]: implemented the =docs/architecture.md= refactor — created the =ortasklib/= package (=core=, =tasks=, =manager=), rewrote =ortask.py=/=orgmgr.py=/=projtui.py= as thin front-ends that no longer import each other (re-exporting moved names so the test suite stays green), then updated =docs/architecture.md= and =CLAUDE.md= to match.
** Gemini [2026-06-12 Fri 21:58]: modified projtui.py to use bold cyan ANSI escape codes for interactive task and project selection prompts.
** Gemini [2026-06-12 Fri 22:28]: added a 12th test case to tests/test_ortask_suite.py verifying the fallback tasks-section creation behavior of cmd_add.
** Claude [2026-06-12 Fri 22:54]: implemented =orgmgr.py migrate= and =projadd= per =docs/orgmgr.md= — added shared =[projects]= registry read/write and single-directory discovery to =ortasklib= (=manager=/=core=), gated =projadd= behind =migrate=, added two XDG-isolated registry tests, and updated =docs/orgmgr.md=/=architecture.md=/=CLAUDE.md= status notes.
** Claude [2026-06-12 Fri 23:17]: redesigned =docs/orgmgr.md= around a master "projdir" of per-project symlink subdirectories (registry on disk, =ortask.ini= just points at the projdir); reframed =projadd= to create symlink subdirs, repurposed =migrate= to record the projdir, and un-deprecated the projdir model. Spec now leads the implementation, which still uses the old name→path registry.
** Claude [2026-06-12 Fri 23:53]: re-implemented the directory-based design in =orgmgr.py=/=ortasklib.manager= — =ortask.ini= now stores a single =projdir=, =resolve_projdir()= prefers it over legacy =projtui.ini=, =projadd= creates per-project symlink subdirs (mirroring target basenames, discovering the .org), and =migrate= records the projdir then deletes =projtui.ini=; replaced the registry tests with projdir/symlink ones and refreshed =docs/orgmgr.md=/=architecture.md=/=CLAUDE.md=.
** Codex [2026-06-13 Sat 00:03]: changed =projtui.py= to resolve selected project task-file symlinks before displaying, reading, editing, or opening them, and added a regression test for canonical task-menu paths.
** Codex [2026-06-13 Sat 00:19]: renamed the global project directory setting to =[projects] registry=, removed live =projtui.ini= and =projdir= fallback handling, switched the CLIs to =--registry=, and updated the tests and docs.
** Codex [2026-06-13 Sat 00:36]: added an =e= editor action to =projtui.py='s file-scoped task-list menu so users can open the current Org file before selecting a specific task.
** Codex [2026-06-13 Sat 00:49]: changed =orgmgr.py list= project summaries to display resolved Org file paths with the home directory collapsed to =~=.
** Codex [2026-06-13 Sat 00:53]: changed bare =orgmgr.py= and =orgmgr.py help= to print command help instead of defaulting to =list=.
** Codex [2026-06-13 Sat 00:56]: added a registry location header to plain =orgmgr.py list= output while leaving JSON output as a project array.
** Codex [2026-06-25 Thu 17:43]: added =docs/templates.md= specifying a future =ortask.py= template-instantiation workflow for creating weekly ElectoramaWeekly tasks from the =* Template= subtree.
** Codex [2026-06-25 Thu 21:30]: revised =docs/templates.md= so =ortask.py apply= defaults to the weekly profile, supports only one top-level =* Template= per file initially, and derives week labels from ISO week Mondays.
** Gemini [2026-06-25 Thu 17:51]: simplified the template specification in =docs/templates.md= by replacing =template instantiate= with the flat =apply= subcommand and automating target week/date defaults.
** Codex [2026-06-25 Thu 22:00]: added bash completion support for =ortask.py=, =./ortask.py=, and the =ort= alias, with README and CLI documentation.
** Claude [2026-06-25 Thu 22:06]: made =docs/templates.md= implementation-ready — generalized the placeholder ordering rule to longest-literal-first (fixing the =twYYWNN= ⊃ =YYWNN= substring-corruption hazard), specified ISO-year/=--week= parsing semantics, required canonical-ID dup checks, handled a missing =* Tasks= section, and added Exit Status / Implementation Notes (parser is =* Tasks=-scoped; template must be read as raw lines) plus matching tests.
** Claude [2026-06-25 Thu 22:48]: implemented =ortask.py apply= per =docs/templates.md= — added =core.find_template_range=/=count_template_sections= and =tasks.resolve_week_target=/=apply_template= (weekly placeholder substitution, ISO week/date math, canonical dup-guard, atomic line insert at the end of =* Tasks=), switched the profile selector to =--template= (default =weekly=), added a verb-naming discussion to the spec, documented =apply= in =docs/ortask.md=/=CLAUDE.md=, and added five pytest cases.
** Codex [2026-06-25 Thu 23:04]: moved bash completion to =misc/ortask-completion.bash= and added completions for =ortask.py apply=.
** Codex [2026-06-27 Sat 18:05]: tightened =docs/taskwarrior.md= into a direct Taskwarrior workflow guide with a short =ortask.py= mapping section.
** Codex [2026-06-27 Sat 18:15]: added =ortask.py -i=/=--interactive= to open =projtui.py='s task menu for the resolved local Org file.
** Codex [2026-06-27 Sat 18:48]: documented the target task-file discovery convention: prefer =task.org=, then exactly one =*.task.org=, then legacy names during an upward search from the current directory.
** Codex [2026-06-27 Sat 19:00]: implemented the new task-file resolver with upward =task.org=/=*.task.org= discovery, legacy fallback, ambiguity errors, and matching tests/docs.
** Codex [2026-06-27 Sat 21:45]: revised =docs/interactive.md= around castabout-derived TUI patterns and added =docs/ecosystem.md= to describe shared =ortasklib= convergence.
** Codex [2026-06-27 Sat 21:56]: changed =ortask.py -i= to show a castabout-style task dashboard with open/done totals before prompting.
** Codex [2026-06-27 Sat 22:06]: documented a future shared menuing layer for =ortask.py -i=, =projtui.py=, and castabout-style workflow tools.
** Codex [2026-06-27 Sat 22:25]: added =ortasklib.menu= dashboard primitives and switched project task views to the same dashboard renderer as =ortask.py -i=.
** Codex [2026-06-27 Sat 22:33]: added castabout-style =Esc= cancellation to =ortasklib.menu= prompts and wired =projtui.py=/=ortask.py -i= menu contexts to use it.
** Codex [2026-06-27 Sat 22:41]: wired =projtui.py='s project picker through =ortasklib.menu= project dashboard rendering and documented the shared project-list path.
** Codex [2026-06-27 Sat 22:52]: updated =docs/interactive.md= to frame arrow-key highlight-bar menus as the decision point for keeping a small prompt_toolkit selector versus adopting an off-the-shelf prompt/TUI layer.
** Gemini [2026-06-27 Sat 23:16]: evaluated the TUI toolkit options and interactive specifications, confirming the choice to use =prompt_toolkit= directly over =InquirerPy=/=Textual=, and updated the design docs.
** Claude [2026-06-27 Sat 23:52]: revised =docs/interactive.md='s highlight-bar decision — kept the highlight bar as the primary interactive target (numbered menu as fallback, not a reason to defer), corrected the dependency argument (InquirerPy/questionary are themselves built on prompt_toolkit), kept prompt_toolkit-direct for in-list hotkeys, set a concrete Textual tripwire, and fixed a machine-specific file:// link to a relative path.
** Claude [2026-06-28 Sun 00:14]: implemented the arrow-key highlight-bar task selector — added =menu.select_menu()=/=MenuResult=/=interactive_select_available()= (inline prompt_toolkit app with j/k+arrow nav, =e=/=t=/=d=/Shift-arrow hotkeys), a =tasks.next_state()= TODO/DONE toggle ring, and wired =projtui.py= =ort -i= to use it with the numbered menu as the non-TTY fallback; added headless pipe-input tests.
** Claude [2026-06-28 Sun 00:39]: fixed t0007 — the highlight-bar selector no longer reorders rows by TODO/DONE state (new =projtui._stable_sort_key= orders by priority/depth/file position only) and re-anchors the highlight on the same task ID across reloads (=projtui._anchor_index=), so toggling no longer slides a different task under the cursor; numbered menu keeps the state-aware order; added tests and updated =docs/interactive.md=.
** Claude [2026-06-28 Sun 00:58]: implemented t0006 — Emacs-style buffered editing for the interactive TUI. New =projtui.OrgBuffer= holds edits in memory, mirrors them to an auto-save sibling =#todo.org#=, and writes the real file only after a "save todo.org? [Y/n]" prompt on leaving a file's context (with crash-recovery on re-entry); one-shot CLI still writes immediately. Threaded the buffer through =projtui= read/write paths, added =core.lines_to_text=, gitignored =#*#=/=.#*=, and added OrgBuffer/save-prompt/recovery tests.
** Claude [2026-06-28 Sun 01:09]: made the save-on-exit prompt three-way — =Esc= now vetoes the exit and returns to the still-running task menu with edits intact (=_resolve_buffer= returns a bool that =task_menu= loops on), only =n=/=no= discards, and =Enter=/=y=/=yes= still saves; updated tests and =docs/interactive.md=.
** Claude [2026-06-28 Sun 01:13]: made the crash-recovery prompt (=_maybe_recover=) three-way to match — =y= recovers, =n=/=no= discards the =#name#= auto-save, and =Esc=/=Enter= keep it untouched to decide later (so only an explicit no throws away recovery data); updated tests and =docs/interactive.md=.
** Codex [2026-06-28 Sun 01:24]: changed interactive task menus to preserve Org file order instead of sorting by state, priority, or depth, keeping authored task hierarchies intact in =ortask.py -i= and =projtui.py=.
** Codex [2026-06-28 Sun 01:32]: implemented =t0005= so =ortask.py add= no longer creates =* Tasks= in arbitrary existing Org files, while still bootstrapping empty dedicated task files.
** Codex [2026-06-28 Sun 01:48]: implemented =t0002= so =ortask.py add= creates =tasks.org= when no task file is discovered and updated task-file discovery to prefer =tasks.org= before =task.org= and =*.task.org=.
** Codex [2026-06-28 Sun 01:51]: changed current task-file discovery terminology from "legacy names" to "compatibility names" for common Org filenames such as =TODO.org= and =todo.org=.
** Codex [2026-06-28 Sun 02:08]: implemented =t0003= so files without =* Tasks= can still be listed, shown, summarized, and edited via valid TODO/DONE task headings across the file.
** Codex [2026-06-28 Sun 11:03]: changed =projtui.py= project-mode task-menu exits to return to the parent project list instead of quitting the whole program.
** Codex [2026-06-28 Sun 11:30]: updated =docs/interactive.md= with Org/Emacs-aligned keybinding guidance, making Shift-Arrow the recommended TODO-state ring control and treating =t= as a transitional alias.
** Codex [2026-06-28 Sun 11:44]: made interactive task menus show all task states by default, removed =t=/=d= row-selector state toggles, added =/= visibility cycling, and updated the interactive docs.
** Codex [2026-06-28 Sun 11:57]: changed the interactive task-menu state hint from oversized shift-arrow glyphs to =Shift+←/→ state=.
** Codex [2026-06-28 Sun 12:26]: added =orgmgr.py -i=/=--interactive= as the public registry-scoped project-browser entry point, documented the =orgm= alias, and extended bash completion for =orgmgr.py=/=orgm=.
** Codex [2026-06-28 Sun 12:59]: switched the =orgmgr.py -i= project picker to the shared prompt_toolkit highlight-bar selector, keeping the numbered project dashboard as the non-TTY fallback.
** Codex [2026-06-28 Sun 14:00]: changed task-list visibility filtering from =/= to =C-t=, reserving slash for future search while keeping Shift-Arrow for TODO-state cycling.
** Codex [2026-06-28 Sun 14:15]: changed selected highlight-bar rows to keep TODO/DONE and project-name colors while using a stronger =▶= row marker.
** Gemini [2026-06-28 Sun 14:34]: subdued the selection bar background to bg:#3a3a3a, fixed highlight continuity across task status and project name fragments, and updated docs.
** Gemini [2026-06-28 Sun 14:46]: implemented state-aware selection highlight backgrounds (gold/olive for TODO, green for DONE, dark gray for other, dark cyan for projects) using 256-color compatible colors to ensure visibility.
** Claude [2026-06-28 Sun 15:45]: reworked the interactive selection bar in =menu.py= so a highlighted row renders as one continuous bright state-colored bar with black text — gold for TODO, green for DONE (gray otherwise, cyan for projects) — replacing Gemini's dark background that left washed-out segments and a same-color-on-same-color (illegible) label; also added a =_selected_bar()= helper, dropped the dead bare =selected= style rule, and updated =docs/interactive.md=.
** Codex [2026-07-18 Sat 01:07 PDT]: Added reusable SUPERSEDED parsing/terminal filtering, TODO-declaration and subtree-state helpers, and symlink-preserving atomic writes for castabout's interactive catch-up workflow, with focused tests.
** Codex [2026-08-14 Fri 12:13 PDT]: made shared interactive task and project selectors scroll long row lists with the highlight while keeping their headers and key hints fixed, with bounded-terminal regression coverage.
** Codex [2026-08-14 Fri 13:07 PDT]: added modal =C-g= command help to shared interactive selectors and structured action metadata so future task commands appear automatically, with UI hints, documentation, and tests.
** Codex [2026-08-14 Fri 13:21 PDT]: unified =Esc=/=b=/=q= as one-level stack-pop commands across help, focus, task, and project menus, removing the multi-level quit path and adding nested-navigation regression coverage.
** Codex [2026-08-14 Fri 14:11 PDT]: added buffered Org priority editing with Shift-Up/Down and an explicit picker, replaced the focus prompt with a highlight-bar task editor, and documented and tested the new workflow.
** Codex [2026-08-14 Fri 21:38 PDT]: added an interactive UI roadmap for replacing repeated one-shot selectors with one bounded 20-row application and added an ordered t0009 task tree for the migration.
** Codex [2026-08-14 Fri 21:59 PDT]: implemented the first bounded-UI roadmap slice with one persistent 20-row task session, in-place task/subtask/Help/confirmation/priority views, external-editor suspension, and focused terminal regressions.
** Claude [2026-08-14 Fri 22:20 PDT]: added a "Risks Observed So Far" section to =docs/roadmap.md= reviewing =ortasklib/menu.py=/=projtui.py= against inedit's module split and the =handrail-plan.md= draft, and updated =~/tmpsorta/tui2026/handrail-plan.md= with findings from that review (outside this repo, not logged separately).
** Codex [2026-08-14 Fri 22:34 PDT]: moved dirty local task-session exit handling into the bounded view stack with Save, Discard, and Continue Editing choices, preserving buffered-edit safety and the numbered fallback.
** Codex [2026-08-14 Fri 22:50 PDT]: moved crash-recovery choices into the bounded task session with non-destructive Enter/Back defaults, explicit Recover/Discard behavior, and matching Handrail planning notes.
** Codex [2026-08-14 Fri 23:11 PDT]: moved the =orgmgr.py -i= project list onto the persistent view stack so project, recovery, task, detail, Help, picker, and save contexts share one bounded application with stable project selection.
** Codex [2026-08-14 Fri 23:25 PDT]: defined controlled normal exit to retain one bounded frame with a factual outcome, made erasure the uncontrolled-exit default, and added real-PTY coverage for long-list scrolling, live resize, Help, terminal restoration, alternate-screen avoidance, and next-prompt placement.
** Claude [2026-08-14 Fri 23:45 PDT]: added a "Task Archiving" section to =docs/roadmap.md= specifying stock Emacs =org-archive-location= behavior (=%s_archive= default, so =todo.org= archives to =todo.org_archive= under =* Archived Tasks= with =ARCHIVE_*= property-drawer metadata), the direction for an =ortask.py archive= verb, and open questions; added terse =t0010= in =todo.org= pointing at it.
** Codex [2026-08-14 Fri 23:51 PDT]: broadened =docs/roadmap.md= into the general ortask roadmap, organized interactive UI and task archiving as peer areas, corrected stale UI status and completion language, and aligned the archive location description with Org's stock =%s_archive::= behavior.
** Claude [2026-08-15 Sat 00:05 PDT]: added =docs/llm-assessments.md= as a peer-feedback page (explicitly not a log) with usage instructions, empty ChatGPT and Gemini sections, and a Claude assessment of the bounded-inline migration flagging ~230 lines of now-callerless compatibility selectors in =ortasklib/menu.py= and the stale "no test suite exists" claims in =CLAUDE.md=/=AGENTS.md=.
** Codex [2026-08-15 Sat 00:16 PDT]: added follow-up tasks for removing obsolete one-shot selectors, refreshing stale agent instructions, assessing interactive performance, and eliminating the standalone =Esc= delay when returning from task details.
** Claude [2026-08-15 Sat 09:34 PDT]: added an "In-App Task Editing" section to =docs/roadmap.md= for near-term editing work, recording that the list view already binds priority picker/shift and a two-entry state ring so the real gaps are a heading-text rewrite helper plus focused text-entry view (no =BufferControl=/=TextArea= exists yet) and a bounded state picker reaching =SUPERSEDED=; added terse =t0014= and =t0015= in =todo.org= pointing at it.
** Codex [2026-08-15 Sat 10:55 PDT]: implemented the first =t0014= slice with a surgical =tasks.change_text()= helper that validates replacement text while preserving heading syntax, formatting, body content, and neighboring lines.
** Codex [2026-08-15 Sat 11:11 PDT]: completed =t0014= with a generic focused =TextInputView= inside the bounded session and a task-detail =TEXT= workflow that validates, buffers, cancels, helps, and saves heading-text edits without launching another application.
** Codex [2026-08-15 Sat 11:31 PDT]: reframed the interactive roadmap around a full Org-backed =orti= issue-editing workspace and added staged tasks for multiline body editing, direct metadata actions, an embedded hierarchical subtask list, and safety coverage.
** Claude [2026-08-15 Sat 12:11 PDT]: Added =t0017= to =todo.org= for the ortasklib half of the SUPERSEDED-to-MOOT rename, naming the =core.py=/=tasks.py=/=menu.py= sites, requiring =SUPERSEDED= to stay a terminal alias so mixed tool versions cannot degrade the keyword into headline prose, and pointing at =castabout/docs/roadmap.md= plus the =t0010= archive overlap.
** Codex [2026-08-15 Sat 12:41 PDT]: completed =t0017= by making =MOOT= the preferred terminal state for declarations and subtree changes, retaining =SUPERSEDED= as a parseable terminal alias, sharing terminal-state dashboard counts, and updating compatibility tests and current documentation.
** Claude [2026-08-15 Sat 14:18 PDT]: Extended =docs/roadmap.md= "Task Archiving" with the =#+TODO:= declaration requirement for the =archive= verb: documented that stock org-archive copies no in-buffer keyword settings (ElectoramaWeekly's fresh archive read only 23 of 64 headings as complete until fixed), and required new archives to carry the source declaration while existing archives merge into theirs as a union across renames rather than a snapshot of current vocabulary.
** Codex [2026-08-15 Sat 17:00 PDT]: made interactive task lists start as top-level overviews with =Tab=/Shift-Tab and Left/Right tree folding, added highlighted =▸=/=▾= disclosure cues, preserved hierarchy and selection through filters and folds, and documented and tested the behavior.
** Codex [2026-08-15 Sat 17:25 PDT]: planned content-fit inline sizing, explicit height and full-screen modes, view-owned sizing preferences, terminal coverage, and Org-compatible reservation of =M-Up=/=M-Down= in the roadmap and =t0018= task tree.
** Codex [2026-08-15 Sat 18:02 PDT]: aligned the adaptive viewport roadmap and =t0018= with Handrail's proposed =Alt-Up=/=Alt-Down= scrolling and =C-^=/=C-6= Resize mode, including inline maximize, full-screen transition, and terminal-test requirements.
** Codex [2026-08-17 Mon 00:24 PDT]: completed =t0016.1= and =t0016.3= by adding surgical task-own-body replacement, a bounded multiline body editor using Enter for newlines and =Ctrl-S= to apply, transitional =TITLE=/=BODY= detail actions, and preservation tests covering Org structure, buffering, recovery, and save flow.
** Codex [2026-08-17 Mon 00:43 PDT]: completed =t0016.2= by replacing task-detail action rows and field subcontexts with one persistent title/body workspace, adding shared multi-control focus and Help lifecycle support, applying both fields atomically to =OrgBuffer=, and reserving the embedded subtask region for =t0016.4=.
** Codex [2026-08-17 Mon 01:11 PDT]: made task-workspace =Ctrl-S= save the entire Org file and reset field undo histories, added an =UNSAVED= footer state and a safe-default Save/Continue/Discard warning on dirty Escape, and documented the distinction between workspace saves and task-list buffering.
** Codex [2026-08-17 Mon 01:33 PDT]: added labeled =OrgBuffer= transaction history for task-list edits, =C-/= undo, =C-r= redo, and =C-s= whole-file checkpoints, plus persistent file-level dirty status in task lists and workspaces and separate =TASK EDITED= signaling for unsaved field text.
** Codex [2026-08-17 Mon 01:43 PDT]: added compact, focusable State and Priority controls to the task workspace, staged their changes with title and body behind the existing save/discard boundary, and covered canonical =TODO=/=DONE=/=MOOT= cycling, priority changes, saving, and discard.
** Codex [2026-08-17 Mon 01:55 PDT]: made task-menu warnings and operational feedback transient for two seconds before restoring the command hint, while preserving final outcomes and guarding newer messages from stale timeout callbacks.
** Codex [2026-08-17 Mon 02:12 PDT]: added a focusable external-editor button and bounded read-only descendant preview to the task workspace, with source-order hierarchy, strict truncation, dirty-edit safeguards, shared workspace activation, documentation, and tests.
** Codex [2026-08-17 Mon 02:34 PDT]: replaced the truncated task-workspace preview with a selectable five-row viewport over all descendants, added shared line/page list navigation and child-workspace stacking that preserves parent context, and updated tests and planning documents.
** Codex [2026-08-17 Mon 02:49 PDT]: made =ort=/=orgm= subcommand registration, help, dispatch tables, Bash completion, and command references alphabetical, established that ordering as contributor policy, and added regression tests.
** Codex [2026-08-17 Mon 03:02 PDT]: completed =t0010= with =ort archive [ID]=, stock-shaped Org archive metadata and level handling, TODO-declaration union, canonical symlink destinations, rollback-protected two-file writes, permanent archived-ID reservation, documentation, completion, and focused tests.
** Gemini [2026-08-18 Tue 13:53 PDT]: created docs/projdirs.md specifying the design of the project directory stack selection tool (pcd) integrated with orgmgr.py and misc/pcd.func.sh.
** Claude [2026-08-18 Tue 14:35 PDT]: simplified =docs/projdirs.md= from 266 to 136 lines: replaced =--print-project-path= (which would break the picker, since =menu.interactive_select_available()= requires an stdout TTY) with a single =--out= channel plus =--edit=, dropped the =projdirs.txt= alternate spelling, cut the 170-line bash prototype down to a contract sketch with the four bugs it contained recorded as notes, and added integration and open-question sections.
** Gemini [2026-08-18 Tue 14:30 PDT]: simplified the bash function prototype in docs/projdirs.md to perform arguments parsing and diffing inline, removing global helper functions to keep the environment namespace clean and avoid over-engineering.
** Gemini [2026-08-18 Tue 14:36 PDT]: implemented orgmgr.py pcd subcommand, created misc/pcd.func.sh, updated autocompletions and docs, and added unit tests.
** Gemini [2026-08-18 Tue 14:44 PDT]: resolved 'emacs -nw' editor launching failures by changing InlineMenuSession.suspend to run synchronously (in_executor=False) and using a bash array in misc/pcd.func.sh.
** Gemini [2026-08-18 Tue 14:51 PDT]: displayed full tildified project directories instead of task files in the project selector, refactoring real_project_path into manager.py.
** Gemini [2026-08-18 Tue 15:01 PDT]: replaced .projdirs file concept with optional * Directories section in the project's Org file, populated todo.org with nowdirs.txt, and updated design and tests.
** Gemini [2026-08-18 Tue 15:14 PDT]: reversed the directory stack loading loop in misc/pcd.func.sh so that the resulting dirstack matches the exact top-to-bottom order specified in * Directories.
** Gemini [2026-08-18 Tue 15:53 PDT]: made double-asterisks, file: prefixes, and org link brackets optional in the * Directories section parser, and updated unit tests.
** Gemini [2026-08-18 Tue 16:05 PDT]: added 'e' key action to the project picker in cmd_pcd to edit directories from the TUI, and added unit tests.
** Gemini [2026-08-18 Tue 16:19 PDT]: added 'edit' and 'dirs' action headers to the pcd output file to distinguish resolution from edit mode in TUI trigger, and updated tests.
** Gemini [2026-08-18 Tue 16:26 PDT]: simplified edit mode detection by directly checking for file existence (-f) on the first target instead of writing custom prefixes, ensuring compatibility with unsourced shells.
** Claude [2026-08-18 Tue 17:12 PDT]: revised =docs/projdirs.md= to drop the =-a= and =-e= shell flags and make the =--out= file mean exactly one thing (the desired dirstack, top first), moving editing entirely inside the helper as a picker key over =InlineMenuSession.suspend()= so no mode signalling crosses back to bash; specified =pushd -n= ordering, argument forwarding instead of shell-side parsing, moving the =* Directories= parser into =ortasklib=, dropping the Org-file bootstrap that violated orgmgr's never-edit-Org rule, and migrating off the =t0011= selector.
** Claude [2026-08-18 Tue 18:05 PDT]: implemented the revised =pcd=: dropped =--edit= and the =-a=/=-e=/=-r= shell flags so the =--out= file only ever carries a directory stack, rebuilt =cmd_pcd= on =InlineMenuSession= with =e= editing inside the session via =suspend()=, moved =* Directories= parsing to =core.parse_directories()= and resolution to =manager.resolve_directories()=, added a private =directories-private.org= per project in the registry with a picker when both it and the task file define a stack, added =core.editor_argv()= shared with =projtui._open_editor()=, and rewrote =misc/pcd.func.sh= to 50 lines that forward arguments and apply the stack with =pushd -n=.
** Gemini [2026-08-18 Tue 22:30 PDT]: automatically merged public and private directory stacks (public on top) instead of prompting, and updated tests and documentation.
** Claude [2026-08-18 Tue 23:31 PDT]: settled the project layer in docs — added =docs/projects.md= (directory-symlink marker replaces the =SKIP_PROJECT_DIRS= blocklist, task files optional, why the symlink registry beat a scan, what may live in a registry entry, =*-private.org= convention, =pmgr add=), added a Project Navigator section to =docs/roadmap.md= (=pmgr=/=ptui= names, rename of =orgmgr.py= rather than =projtui.py=, verb renames, one shared project view, implementation order), refreshed =docs/ecosystem.md= to two commands and a library, shrank =docs/orgmgr.md='s registry model and recorded that =scan= is not planned, pointed =docs/interactive.md='s workspace discovery at the new model, noted the move in =docs/architecture.md= and =docs/projdirs.md=, fixed =CLAUDE.md='s stale test-suite and =scan= claims, and added =t0019= with six subtasks.
** Claude [2026-08-19 Wed 00:11 PDT]: implemented the project layer (=t0019=) — replaced =manager.SKIP_PROJECT_DIRS= with an outward-symlink marker in new =read_project_entry()=, made task files optional (=Project.org_file= may be =None=, plus =link=/=warning= fields) so task-less, broken, and ambiguous entries stay listed, added =manager.project_root_for()= and =core.preferred_task_file_in()= for =add='s upward walk, renamed =orgmgr.py= to =projmgr.py= (aliases =pmgr=/=ptui=) with =projadd=/=migrate= becoming =add=/=init= (old names kept as deprecated aliases) and new =rm= and =doctor= verbs, moved =projtui.py= to =ortasklib/taskui.py= with its project browser folded into =projmgr.py= so no script imports another, converged the navigator and =pcd= pickers onto one =_project_view=/=_project_rows= (=menu.ProjectRow.org_file= is now =location=), and updated the completion script, =misc/pcd.func.sh=, and every doc naming the old files.
** Gemini [2026-08-19 Wed 12:00 PDT]: renamed pcd references to cdproj across all documentation and tests, renamed docs/projdirs.md to docs/cdproj.md, and sorted subcommand registrations alphabetically.
** Gemini [2026-08-19 Wed 12:08 PDT]: made cdproj command accept an optional project name positional argument to resolve stack directly without TUI, updated shell script and test suite.
** Claude [2026-08-19 Wed 13:12 PDT]: made =cdproj= tab-completion offer project names — added =projmgr.py list --format names= (bare names, one per line, no task-file parsing) as the completions' single view of the registry so the outward-symlink marker rule stays in Python, added =_projmgr_projects()= and a dedicated =_cdproj_complete()= to =misc/ortask-completion.bash= (the =cdproj= shell function supplies its own subcommand and =--out=, so it cannot share =_projmgr_complete=), registered completion for =cdproj=, completed project names for =pmgr cdproj= and =pmgr rm= and files for =--out=, corrected the stale usage header in =misc/cdproj.func.sh=, documented all of it in =docs/projmgr.md=/=docs/cdproj.md=/=docs/ortask.md=/=README.org=, and fixed =pcd=/=orgm= names the rename missed in =docs/roadmap.md=, =ortasklib/manager.py=, and the test suite.
** Claude [2026-08-19 Wed 13:27 PDT]: respecified private vs project =* Directories= resolution in =docs/cdproj.md= (new "Which list wins" section) and =docs/projmgr.md= — the private list now wins outright and sets the order instead of being merged below the project list, with a stderr warning naming each project entry the private list lacks, comparison by resolved path, dedup keeping the first occurrence, an empty private section counting as a list that exists, and identical behavior across the picker, =cdproj PROJECT=, and the numbered fallback; docs only, code still merges. Also fixed the stale =docs/projdirs.md= link in =docs/projects.md=.
** Claude [2026-08-19 Wed 13:32 PDT]: implemented the private-wins =* Directories= rule specified in the previous entry — new =_CdprojSession.resolve_stack()= is the single place a stack is resolved (private list wins outright and sets the order, project entries it lacks become warnings compared by resolved path, =_dedupe()= keeps first occurrences), =write_selection()= and =report()= route the picker, =cdproj PROJECT=, and the numbered fallback through it, deleting the copy-pasted merge in =_cdproj_fallback= and the nullable-session hack in =select_stack= (which also fixes the fallback silently swallowing write errors); warnings go to stderr after the session exits, exit status is unchanged, and the picker footer now names the winning list. Replaced the merge-order test with six covering precedence, order, resolved-path comparison, dedup, an empty private section, and all routes agreeing.
** Gemini [2026-08-19 Wed 13:40 PDT]: provided recommendations in =docs/gemini-ortask-design.org= to make =ptui= and =cdproj= visually distinct and updated ID format examples and regex.
** Claude [2026-08-19 Wed 14:03 PDT]: made =ptui= and =cdproj= visually distinct per Gemini's recommendations — new =_ProjectListing= holds the per-surface title, row label, and third column (=Project navigator=/=PROJ=/open task count vs =Change directory=/=CD=/directory plus the =[private]=/=[project]= list that will set the stack), threaded through =_project_rows=, =_project_view=, and both numbered dashboards; =menu.ProjectRow.location= became =detail= with a =detail_header= argument, and row styling now keys off new =menu.PROJECT_ROW_LABELS= rather than the literal ="PROJECT"=. Skipped the dynamic per-scroll footer as more machinery than the problem needs. Also fixed a latent bug the marker exposed: rich read cell text as console markup, so =[private]= and any task title containing =[#A]= were silently dropped from the numbered dashboards.
** Claude [2026-08-19 Wed 18:04 PDT]: stopped the project menus obscuring project locations — the summary line now follows the highlight, naming the selected project's directory and task file (=_project_summary()=, wired through =_project_view= as =status_text=), and =Registry:= moved to a new right-aligned =menu.MenuView.title_right= rendered opposite the title by =_render_header=/=_right_gap()= (dropped whole when the terminal is too narrow, never truncating the left). Put it on the title line rather than the summary line because the title is fixed, so the registry no longer flickers as the cursor passes projects with longer paths. The numbered dashboards have no highlight to describe, so =_ProjectListing= gained =dashboard_detail= and the navigator's table shows =~/src/elusync  (3 open)= instead of the count alone. Recorded the principle as "Do not obscure the location of stuff" in =docs/interactive.md=.
** Claude [2026-08-20 Thu 15:46 PDT]: fleshes out =docs/config.md= from robla's stub into a full reference — states the organizing rule (one machine-global setting, the registry location; everything else is a file found from where you are), tabulates what is configured where, documents =ortask.ini= and the four environment variables (=ORTASK_FILE=, =XDG_CONFIG_HOME=, =ORTASK_PROJMGR=, =VISUAL=/=EDITOR=), gives the verified task-file precedence ladder (=tasks.org=, =task.org=, one =*.task.org=, =TODO.org=, one =TODO*.org=, =todo.org=), and describes the per-project =* Directories= layer and what "private" does and does not mean. Answers the open one-file-vs-many question in robla's favor of many: a central =projects.org= would force =projmgr.py= to become an Org editor (=pmgr rm= could no longer be =rm -r entry=) and would reintroduce a second source of truth beside the outward-symlink marker, so pointers stay symlinks, config stays per-entry, and prose goes in a registry-root README no tool reads. Records two gaps: =*-private.org= is reserved in =docs/projects.md= but =manager.choose_org_file= excludes only the literal =directories-private.org=, so =notes-private.org= is still chosen as a project's task file (verified), and =CLAUDE.md=/=GEMINI.md= describe the resolution ladder wrongly (both treat =tasks.org= as a legacy fallback when it is the first name probed). Docs only; no code changes.
** Claude [2026-08-20 Thu 16:12 PDT]: records in =docs/config.md= the decision to replace per-entry =directories-private.org= with one =projects.org= at the registry root (symlinks stay the pointers), names =~/Projects= the standard registry, and surveys Python Org libraries — recommending none at runtime, =orgparse= in tests only.
** Gemini [2026-08-20 Thu 22:08 PDT]: updated docs/config.md to remove org-parser and PyOrgMode library options, added panflute, and clarified python version constraint explanation for orgformat.
** Gemini [2026-08-20 Thu 23:02 PDT]: updated docs/config.md to add org-rw to the libraries table (with investigation notes) and reordered the table based on user preferences (orgmunge at top).
** Gemini [2026-08-20 Thu 23:11 PDT]: broke out Org library assessment into =docs/orglib.md=, covering bespoke surgical editing in =ortasklib= and evaluating external Python libraries.
** Claude [2026-08-20 Thu 23:35 PDT]: adds =orglib= shim design, a phased orgmunge migration gated on a measured per-file round-trip check, and an optional =orgparse= conformance test to =docs/orglib.md=; records that orgmunge alters three things (keyword case, tag padding, blank lines before headings) and that =heading.todo == X= is a silent no-op.
** Gemini [2026-08-20 Thu 23:52 PDT]: trimmed =docs/orglib.md= to use an objective NPOV tone and added the "Assessments by LLMs" section with Gemini's assessment and placeholders for ChatGPT and Claude.
** Codex [2026-08-21 Fri 00:01 PDT]: assessed the proposed =orglib= abstraction in =docs/orglib.md=, recommending a narrow internal parsing/patch boundary and conformance corpus before runtime backend switching, with explicit fidelity gates for =orgmunge= and direct Tree-sitter retained as a future experiment.
** Claude [2026-08-21 Fri 00:25 PDT]: adds Claude's assessment to =docs/orglib.md= — measures orgmunge round-trips across the real corpus (2 of 6 ortask-owned files exact, both =llm-log.org= copies unparseable), corrects my earlier claim that the per-file fidelity preflight could phase writes in, and proposes an order that puts a =#+TODO: TODO MOOT | DONE= line and test-only readers ahead of any refactor.
** Gemini [2026-08-21 Fri 00:38 PDT]: fixed header formatting and updated Gemini's assessment in =docs/orglib.md=, adding length guidelines for contributing models.
** Codex [2026-08-21 Fri 00:47 PDT]: shortened and depersonalized its =docs/orglib.md= assessment, clarified the long-term capability-based backend direction and staged migration, and revised the contributor guidance to welcome labeled architectural speculation grounded in current evidence.
** Claude [2026-08-21 Fri 01:05 PDT]: adds =ortasklib/orglib/= with a read-only bespoke backend (=parse=, =Document.tasks=, =Document.render=) and routes =manager.summarize_projects= and =projmgr._project_tasks= through it; three tests, including =render(text) == text= as the fidelity property a second backend must match.
** Claude [2026-08-21 Fri 01:15 PDT]: moves =orglib= out of =ortasklib= to a top-level peer package (=import orglib=), re-exporting =TodoItem= so callers can migrate names before the code moves; notes in =docs/orglib.md= that the dependency still points orglib -> ortasklib.core and what reversing it would cost.
** Claude [2026-08-21 Fri 01:26 PDT]: reverses the =orglib= <-> =ortasklib= dependency by moving the Org syntax (regexes, =TodoItem=, =find_tasks_range=, =parse_org=, =parse_directories=) from =ortasklib/core.py= to =orglib/syntax.py=, which =core= re-exports so no call site changed; =orglib= now imports only the stdlib, enforced by a subprocess test that blocks =ortasklib= at import.
** Claude [2026-08-21 Fri 01:34 PDT]: updates =docs/architecture.md= (dependency graph, =orglib/= section, package layout), =docs/testing.md=, =docs/templates.md=, =docs/cdproj.md=, =docs/config.md=, =docs/ecosystem.md=, =CLAUDE.md=, and =GEMINI.md= for the =orglib= split, and adds =t0020=-=t0026= to =todo.org= covering the remaining parsing migration, orgparse/orgmunge experiments, an in-file =#+TODO:= line, a license, the =*-private.org= discovery bug, and the registry index file.
** Codex [2026-08-21 Fri 01:46 PDT]: reviewed =t0020=-=t0026= without altering them and added =t0027=-=t0029= for an independently consumable =orglib= boundary, an explicit generic-Org-versus-ortask-policy decision, and a mutation conformance matrix for bespoke and external backend experiments.
** Codex [2026-08-21 Fri 01:49 PDT]: corrected =t0021=-=t0024= and =t0026= after review: Org declarations now place =DONE=/=MOOT=/=SUPERSEDED= after the terminal-state bar, optional backend tests use hermetic fixtures, licensing no longer blocks test-only evaluation, and the registry index uses a one-time cutover; corrected the repeated keyword examples in =docs/orglib.md=.
** Codex [2026-08-21 Fri 02:07 PDT]: redesigned the planned =orglib= API around source-backed regions with file and ancestor context, exact patches versus explicit bounded replacement, source-revision and boundary checks, and synthetic standalone documents for external parsers; refined =t0022=/=t0028=/=t0029= and added =t0030= for a test-only brokered orgmunge editing prototype.
** Gemini [2026-08-21 Fri 16:05 PDT]: specified directory stack persistence (cdproj -s / pmgr set-dirs) in =docs/cdproj.md=, including current-project auto-detection, ~ formatting, and interactive subtraction prompts.
** Gemini [2026-08-21 Fri 16:09 PDT]: added task =t0031= to =todo.org= for saving shell directory stacks via =cdproj -s= / =pmgr set-dirs=.

** Claude [2026-08-21 Fri 16:26 PDT]: retargets the =set-dirs=/=cdproj -s= spec at the registry index =projects.org= across =docs/cdproj.md=, =docs/config.md=, =docs/projmgr.md=, and =docs/projects.md=, resolves the =--stdin=-versus-prompt conflict with argv input plus =--missing=, and splits =t0031= into =t0031.1=/=t0031.2= behind =t0026=.
** Codex [2026-08-21 Fri 16:53 PDT]: integrated =pmgr migrate= into the =t0026= to =t0031= plan, making =projects.org= the explicit migration marker, specifying a validated and resumable no-fallback cutover, and settling =set-dirs --project= plus zero-input errors.
** Codex [2026-08-21 Fri 17:35 PDT]: implemented =t0026.1= with source-backed =orglib.Document.directories(project)= lookups, explicit missing/empty states, duplicate-heading errors, exact project and section spans, and focused parser contract tests while leaving legacy consumers unchanged.
** Gemini [2026-08-21 Fri 17:42 PDT]: rewrote =docs/gemini-ortask-design.org= to match the current multi-tool architecture and recommend replacing the "doctor" verb with "check"/"audit", and updated Gemini's assessment in =docs/llm-assessments.md=.
** Codex [2026-08-21 Fri 17:49 PDT]: implemented =t0026.2= by reclaiming =pmgr migrate= from =init=, adding validated dry-run and atomic index creation, exact resumable cleanup, shell completion, fixture coverage, and migration-status documentation while leaving consumer cutover to =t0026.3=.
** Claude [2026-08-21 Fri 18:19 PDT]: annotated =docs/claude-ortask-design.org= with what shipped since 2026-04-08 and fixed its stale design-doc links, refreshed Claude's =docs/llm-assessments.md= section, and clarified =t0026.3= to lead with the unmigrated read path in =manager.directory_candidates()= — on the now-migrated registry every private stack resolves to nothing until that lands.
** Codex [2026-08-21 Fri 18:33 PDT]: completed =t0026.3= and =t0026= by cutting =cdproj= over to source-backed =projects.org= sections, enforcing missing and mixed migration gates, adding bounded private-section initialization and =doctor= index diagnostics, refreshing the migration docs and ChatGPT assessment, and expanding regression coverage.
** Codex [2026-08-21 Fri 19:21 PDT]: implemented =t0031.1= with =pmgr set-dirs= project inference, positional and stdin stacks, canonical path deduplication, keep/remove/cancel subtraction policy, dry-run and migration gates, source-preimage checks, one bounded atomic index rewrite, Bash completion, documentation, and fixture coverage.
** Codex [2026-08-21 Fri 19:28 PDT]: completed =t0031= with =cdproj -s=/=--save= forwarding the live shell directory stack to =pmgr set-dirs= without changing shell state, project-name completion, shell-level regression tests, and updated usage documentation.
** Codex [2026-08-21 Fri 20:12 PDT]: added the canonical Org-file path as right-aligned =File:= context across =orti= task menus and workspaces, matching =ptui='s registry header while preserving the shared narrow-terminal fallback.
** Codex [2026-08-21 Fri 20:41 PDT]: replaced =cdproj='s =[private]=/=[project]= row labels with aligned effective stack counts, a trailing =*= for registry-defined custom stacks, and the explicit =* = custom= footer legend while sharing count resolution with the load path.
** Codex [2026-08-21 Fri 20:51 PDT]: refreshed =AGENTS.md= for the implemented two-CLI, =ortasklib=/=orglib=, optional-TUI, pytest-backed repository and split =t0012= into completed Codex guidance plus pending Claude and Gemini follow-ups.
** Claude [2026-08-21 Fri 21:05 PDT]: refreshed =CLAUDE.md= for the current suite — corrected the task-file resolution ladder, the heading regex, and both verb lists; recorded the =projects.org= registry index, =set-dirs=, =misc/= shell integration, optional TUI dependencies, and the open =t0020=/=t0023= gaps; added data-safety and log-format rules; closed =t0012.2=.
** Gemini [2026-08-21 Fri 22:48 PDT]: refreshed =GEMINI.md= for the current repository architecture, resolution ladder, and subcommands; closed =t0012.3= and =t0012=.

** Claude [2026-08-21 Fri 23:00 PDT]: closed =t0011= by deleting =_run_selector=, =select_menu=, and =select_project_menu= (=menu.py= 1357 → 1124 lines) plus their three compatibility tests, moved the empty-list contract onto an =InlineMenuSession= test, and updated =docs/architecture.md=, =docs/interactive.md=, =docs/roadmap.md=, =docs/cdproj.md=, and Claude's assessment.
** Codex [2026-08-21 Fri 23:08 PDT]: added =ort init= to create a local empty =tasks.org= without upward discovery, with dedicated-name validation, explicit/environment overrides, exclusive missing-file creation, nonempty-file refusal, alphabetical help/completion, documentation, and fixture coverage.
** Claude [2026-08-21 Fri 23:28 PDT]: added =docs/logging.md= specifying a disposable JSON Lines event log under =$XDG_STATE_HOME/ortask/log/= with append-only writes, =write=/=activity= levels, and =ort log=/=pmgr log= readers, and =docs/extensions.md= defining a pull-first extension contract over =--format json= plus a zim journal section grounded in the installed notebook's layout, granularity, and page conventions.
** Codex [2026-08-21 Fri 23:55 PDT]: specified =ptui= as a project-steering dashboard with =projects.org= priority and description metadata, priority/alphabetical/modified sorting, buffered metadata editing, non-normative task-file mirrors, bounded-write safety, and a staged implementation plan; linked a concise summary from =docs/projmgr.md=.
** Claude [2026-08-22 Sat 00:28 PDT]: revised =docs/logging.md= per robla's decisions — the log moves to =<registry>/log/YYYY-MM.jsonl= beside =projects.org= (reserved name, symlinkable, gitignore note), an =[log] enabled= switch in =ortask.ini= defaulting to off with a per-project =:LOG:= property edited from =ptui='s metadata workspace later, =cdproj= logged as a deliberate switch while navigator browsing is not, and manual =ort log "note"= entries recorded as someday-maybe; opened the =repair=/=doctor= naming question.
** Claude [2026-08-22 Sat 00:44 PDT]: added =t0033= with five shovel-ready subtasks for the event log (writer, write-path events, =cdproj= switches, =ort log=/=pmgr log= readers, reserved =log/= plus configuration docs) and =t0034= for a first zim journal extension.
** Codex [2026-08-22 Sat 00:49 PDT]: added =t0035= with five staged subtasks for =ptui= project metadata parsing, priority-first rows and sorting, buffered priority writes, a compact metadata workspace, and modified-time plus task-file-mirror diagnostics; used the next free ID after Claude also assigned =t0034=.
** Codex [2026-08-22 Sat 01:10 PDT]: completed =t0033= with opt-in capped append-only JSON Lines events for task/project writes and =cdproj= switches, saved-TUI diff logging, =ort log=/=pmgr log= time/project filters and plain/raw-JSON/Org output, reserved =log/= handling, preserved multi-section config, Bash completion, documentation, and focused concurrent/CLI regression coverage.
** Claude [2026-08-22 Sat 01:34 PDT]: added =t0036= for the priority-cookie matching gap that silently detaches a project's directory stack, and sharpened =t0035.1=–=t0035.4= with their dependency, sort-mode staging, and the two overlapping editors; recorded the cookie hazard, the buffered-versus-preimage write decision, and the property drawer in =docs/ptui.md= and =docs/config.md=.
** Claude [2026-08-22 Sat 01:50 PDT]: recorded robla's write-path decisions in =docs/ptui.md=, =docs/config.md=, =t0035.3=, and =t0035.4= — =ptui= holds one =projects.org= buffer, a stale save refuses with a recoverable error, =#projects.org#= is reserved, the metadata workspace edits directories through the same transaction as =cdproj='s external editor, and the priority cookie stays Org-idiomatic — and added =t0037= for alerting on and then automerging external index changes.
** Claude [2026-08-22 Sat 02:00 PDT]: implemented =t0036= — =orglib.syntax._project_title= now strips a leading priority cookie as well as trailing tags, so =* [#A] ortask= keeps matching registry entry =ortask= and keeps its private directory stack; added four regression tests covering the parser, =cdproj=, =set-dirs= over a cookie and property drawer, and =migrate=, and closed the hazard notes in =docs/config.md= and =docs/ptui.md=. Restated =t0037.2= as an in-memory merge that may refresh =#projects.org#= but never writes =projects.org=.
** Codex [2026-08-22 Sat 02:08 PDT]: made =t0037= shovel-ready as three reviewable stages covering exact external-change detection, a pure Base/Ours/Theirs project-section merge planner, buffer rebasing and conflict UX, with explicit save, undo, auto-save, event-log, source-fidelity, and no-pre-save-write contracts in =docs/ptui.md=.
** Claude [2026-08-22 Sat 02:19 PDT]: finished the =t0036= fix by routing =manager._index_project_headings= through a public =orglib.syntax.project_heading_name()=, so =doctor= no longer reports a cookied section as stale, and implemented =t0035.1=: =orglib= gains =ProjectSection= with priority, tags, ordered properties, and heading/drawer/subtree spans, reached through =Document.project()=/=projects()=, plus =manager.read_project_metadata()= joining index metadata onto registry entries and warning per project rather than failing the list.
** Codex [2026-08-22 Sat 02:34 PDT]: implemented =t0035.2= with priority-first =ptui= rows showing priority, open-task count, description, and metadata warnings; added =s= cycling between Priority and Alphabetical in prompt-toolkit and numbered modes with name-anchored selection, manager-owned sort policy, documentation, and regression coverage.
** Codex [2026-08-22 Sat 11:58 PDT]: implemented =t0035.3= with buffered Shift-Up/Down project-priority edits, source-preserving heading rewrites, name-anchored resorting, visible dirty state, undo/redo, exact-preimage-safe saves, save/discard/continue exit handling, =#projects.org#= recovery, and concurrent =pmgr set-dirs= regression coverage.
** Codex [2026-08-22 Sat 12:48 PDT]: implemented =t0037.1= with reusable file signatures and structured external-change state in =OrgBuffer=, periodic =ptui= polling, clean index adoption and row refresh, persistent dirty-buffer alerts, mandatory exact save checks, and no-loss coverage for changed, touched, replaced, missing, unreadable, and same-signature files.
** Codex [2026-08-22 Sat 12:57 PDT]: implemented =t0037.2= as a pure Base/Ours/Theirs project-index merge planner that replays exact non-conflicting local sections onto the external canvas, preserves external preamble/order/add/delete changes, returns typed conflicts and replay/absorption metadata, validates malformed structures, and performs no file I/O.
** Codex [2026-08-22 Sat 13:19 PDT]: completed =t0037.3= and =t0037= with in-memory =ptui= rebasing of disjoint external project-index edits, one-step rebased undo and auto-save recovery, named Reload/Retry/Continue conflict handling, final preimage-safe saves, external-aware event attribution, updated concurrency documentation, and focused race/recovery coverage.
** Codex [2026-08-22 Sat 13:32 PDT]: added medium/low-priority =t0038= refactoring stages for a format-neutral transactional file buffer, generic =orglib= keyed-region merging, a thin project-index adapter, and evidence-gated standalone packaging; marked the corresponding code boundaries to discourage further =ortasklib= coupling.
** Codex [2026-08-22 Sat 13:58 PDT]: completed =t0038.1= by extracting exact-text transactions, auto-save, external-change detection, rebasing, and preimage-safe saves into dependency-free peer =textbuffer.py=; retained =taskui.OrgBuffer= as the Org/logging adapter, added direct dependency/hook tests, and updated architecture guidance before =t0035.4=.
** Codex [2026-08-22 Sat 14:22 PDT]: completed =t0035.4= with an =m= project-metadata workspace for priority, description, task-file mirror refresh, custom directories, and =$VISUAL= handoff; composed fields into one reconciled =OrgBuffer= save, moved =set-dirs= and =cdproj e= onto the shared bounded directory writer with stale-source checks, and added source-fidelity, transaction, and external-editor regression coverage.
** Codex [2026-08-22 Sat 15:06 PDT]: completed =t0035.5= and parent =t0035= with Priority/Alphabetical/Modified project sorting, newest-readable-task-file ordering, name-anchored mode changes, semantic non-authoritative =TASK_FILE= mismatch diagnostics in project rows and metadata context, explicit staged refresh feedback, updated dashboard documentation, and focused regression coverage.
** Codex [2026-08-22 Sat 16:06 PDT]: made =pmgr add= register project-only entries when Org discovery is ambiguous, preserved explicit =--file= selection, stopped registry reads from guessing among candidates, and documented and tested the optional-task-file behavior.
** Codex [2026-08-22 Sat 16:58 PDT]: added explicit navigation and edit modes to shared workspaces so arrows traverse every field until Enter enables editing, applied the behavior and visual cues to task and project-metadata views, and updated interaction tests and documentation.
** Gemini [2026-08-22 Sat 22:28 PDT]: documented a hybrid sort and filter proposal in =docs/interactive.md= and =docs/gemini-ortask-design.org= combining inline quick-toggles with a dedicated view configurator.
** Gemini [2026-08-22 Sat 22:37 PDT]: added task =t0039= with four subtasks to =todo.org= for staged implementation of the hybrid sort and filter interface.


** Claude [2026-08-22 Sat 22:48 PDT]: revised Gemini's hybrid sort and filter section in =docs/interactive.md= against the shipped bindings — dropped =Tab= (fold), kept the three-way filter cycle, put the view screen on the existing =WorkspaceView=, moved direction inside the sort key, and deferred =orti= sorting for the tree invariant — and reworked =t0039= into six staged subtasks.
** Claude [2026-08-22 Sat 23:07 PDT]: implemented =t0039.1= and =t0039.2= — new =ortasklib/viewstate.py= holds the filter, sort, and direction model with =order_by()= inverting only the primary axis so unreadable projects stay last and tie-breaks stay ascending; both quick toggles moved onto it, =ptui= gained a =C-t= project filter that keeps unreadable projects visible, the task filter label became =DONE+MOOT= to name every terminal state it matches, and both footers now carry one badge.
** Codex [2026-08-22 Sat 23:27 PDT]: reviewed the =t0039= design, kept the shipped quick toggles, made dependency/reversal/render-snapshot cleanup the next stage, deferred =v= until it justifies another UI, preserved =C-s= as disk save, and recorded truthful terminal-label, sibling-sort, and Org-tag semantics.
** Codex [2026-08-22 Sat 23:37 PDT]: corrected the =t0039= plan to retain the dedicated =v= sort/filter screen as a committed feature after boundary cleanup, while preserving quick toggles, live session state, disk-save semantics, and a separate prompt-toolkit adapter.
** Claude [2026-08-23 Sun 00:08 PDT]: completed =t0039.3= and =t0039.4= — =viewstate.py= is now stdlib-only with a meta-path test blocking =core=, =orglib=, and =prompt_toolkit=; task filter policy moved to =taskui=, project axes to =projmgr=, and the prompt_toolkit form to a new =viewui.py=; reversibility is declared per sort with named directions (=A-Z=/=Z-A=, =newest first=/=oldest first=) so file order can never be inverted; each render reads a project task file once via =manager.snapshot_projects()=; the terminal filter now reads =DONE+= with all three states named in help; and =v= opens a live-applying view screen where =C-s= still saves the file.
** Claude [2026-08-23 Sun 00:22 PDT]: completed =t0039.5= — =taskui.sibling_ordered_items()= walks the task tree depth-first and sorts only each sibling group, so Priority and Title orders move rows without detaching children from parents; source order stays the final tie-break, file order alone is not reversible, =s= now cycles File/Priority/Title in =orti=, and the task badge takes the whole view rather than rebuilding one from the filter, which had been silently dropping the order.
** Claude [2026-08-23 Sun 00:38 PDT]: added a modification-age column to the =ptui= project rows and numbered dashboard, so the Modified sort key is visible rather than taken on faith — compact relative ages (=now=, =9m=, =3d=, =5w=, =2y=) read from the render's existing snapshot, shown in every order, with =-= when a task file cannot be read and columns joined so an overflowing workload column cannot run into the age.
** Claude [2026-08-23 Sun 00:47 PDT]: made =orti= start on open tasks, matching what =ortask.py list= already selects by default — =cmd_interactive= now passes a filter mode instead of hardcoding =include_done=True=, and =task_menu=/=local_file_menu=/=InteractiveTaskController= accept =filter_mode= so =list='s =--todo=/=--done=/=--all= reach the menu too; =C-t= and =v= still change it live.
** Claude [2026-08-23 Sun 15:47 PDT]: fixed =t0013.1= — the standalone =Esc= delay was prompt_toolkit's 0.5s =ttimeoutlen=, which =InlineMenuSession= never set (measured: Esc 514ms vs =q=/=b= at 8ms); the inline application now waits =menu.ESCAPE_FLUSH_SECONDS= (50ms), which still assembles an escape sequence torn in two by a slow link, with =ORTASK_ESC_TIMEOUT= to widen it further; also repaired the stale =test_projmgr_interactive_uses_registry_project_menu= left red by the =include_done= default change.
** Claude [2026-08-23 Sun 16:35 PDT]: widened the project name column from 12 to 14 in both =ptui= and the =cdproj= picker, so a 13-character name like =bashfuncs2023= no longer pushes its own row's workload, age, and description one column right of every other row; the width is now one shared =projmgr.PROJECT_NAME_WIDTH= rather than two literals.
** Gemini [2026-08-23 Sun 18:41 PDT]: addressed =t0032= in specs by replacing =pmgr doctor= with =pmgr repair= in =docs/projmgr.md=, adding the =info= verb to =docs/projmgr.md= and =docs/ortask.md= with active project name detection, and updating =tasks.org=.

** Claude [2026-08-23 Sun 19:04 PDT]: made =t0032= implementable — reworked its =tasks.org= entry from Gemini's past-tense summary back into an imperative task with the decisions recorded and five subtasks; specified =repair= as asking before each fix with =--force= (not =--fix=, matching =add=/=rm=) and refusing unattended runs without a TTY, one exit rule of 0/2/1 across both tools, and =info= as strictly read-only with silent single-field forms for prompts; dropped =--name= from =ort info= since the registry is the project layer's subject; marked both new doc sections =Status: specified, not implemented=; and filed three bugs — =t0040= the =cdproj= picker never returns the project it chose, =t0041= =pmgr --todo-only= is inert, =t0042= a broken project's warning prints twice in the numbered dashboard.
** Gemini [2026-08-23 Sun 19:38 PDT]: completed =t0032= (=t0032.1=–=t0032.5=) — renamed =pmgr doctor= to =pmgr repair= (preserving =doctor= as a deprecated alias for =repair --dry-run=); unified the exit code matrix (0 clean, 2 problems remain, 1 unreadable subject/no-TTY refusal) and safety gating with =--force= across both tools; added =ort info= reporting file metadata, provenance, line spans, counts via =TERMINAL_STATES=, ID schemes, and archive status (with =--file= and =--format json=); added =pmgr info= reporting resolved project context, canonical task file, mirror mismatch, stack source, and snapshot task counts (with single-field flags =--name=, =--path=, =--file= that fail silently with exit 1 on no match, and =--format json=); and updated completion and test suites.
** Claude [2026-08-23 Sun 20:54 PDT]: reviewed Gemini's =t0032= implementation and filed what it turned up without touching code — =t0043= the no-TTY refusal fires unconditionally, so =ort repair= is now a hard error in scripts although no fix exists to confirm, and exit 1 conflates that with an unreadable subject; =t0044= =pmgr info= reports an unreadable index as =Directories: none=, prints a reconstructed heading for a registry with no =projects.org=, and returns zero counts in JSON where its own text says "unreadable"; =t0045= =ort info= buckets =SUPERSEDED= into an unlabelled "other" and carries its own heading and =#+TODO:= regexes instead of =orglib='s. Corrected the ambiguity in =docs/projmgr.md= that produced the first — the refusal is conditional on having a fix to offer — and refreshed the stale =repair= status in =docs/ortask.md=.
** Claude [2026-08-29 Sat 22:37 PDT]: Recorded that a task ID is never repurposed, not just never reused --- redefined work closes the old ID =DONE= or =MOOT= and opens a new one, while reopening or rewording a task stays ordinary curation. Added to the ID rules and the Conventions list in CLAUDE.md, after a gitmgr task had its subject rewritten into a different task under the same ID.
** Codex [2026-08-31 Mon 22:47 PDT]: specified always-confirmed global =C-x= exit for =orti=/=ptui= with controller-owned dirty/history/conflict resolution, ptui multi-buffer handling, root-Back safety, roadmap sequencing, and staged implementation task =t0046=.
** Codex [2026-08-31 Mon 23:09 PDT]: implemented =t0046.1= as an opt-in =InlineMenuSession= global-exit gateway with clean confirmation, safe-default Continue, root-Back routing, repeated-=C-x= protection, active-field restoration, and focused prompt-toolkit tests; app wiring remains gated on dirty-state providers.
** Codex [2026-08-31 Mon 23:26 PDT]: implemented =t0046.2= by enabling confirmed global exit in standalone =orti=, reporting and resolving task workspace drafts, buffered edits, history, recovery data, and external conflicts through controller-owned save/discard hooks, while preserving failed-save work and recovery-screen Back behavior.
** Codex [2026-08-31 Mon 23:41 PDT]: revised =t0046= after hands-on review so clean =orti= exits are immediate again and dirty exits preserve the current body while replacing only the footer with an inedit-style =Y=/=N=/=C-c= question; removed the obsolete modal concern display and retained controller-owned save, discard, conflict, and recovery safety.
** Codex [2026-08-31 Mon 23:54 PDT]: implemented =t0046.3= by enabling ptui's footer exit gateway with project-index-first aggregation of retained =projects.org= and active task-file state, metadata-draft application, all-files preflight before writes, controller-specific discard, partial-save baseline hooks, and project recovery as a child context.
** Codex [2026-09-01 Tue 00:11 PDT]: completed =t0046.4= by listing =C-x= in selector, text, multiline, and workspace contextual Help only where the application opts in, adding a width-gated compact =C-x exit= footer hint, naming still-pending files alongside already-saved ones after a partial multi-file write, and covering the exit lifecycle with pipe-input and PTY tests across nested menus, Help, conflict views, active fields, and a real dirty-exit terminal cycle.
** Claude [2026-09-01 Tue 00:33 PDT]: made Right open the highlighted task in =orti= and =ptui= once there is nothing left to expand — a leaf, an already-expanded parent, or a read-only heading — since Down already reaches the first child; updated the command strip, contextual Help, =docs/interactive.md=, and the tree-navigation tests.
** Claude [2026-09-01 Tue 00:44 PDT]: bound Right to open the highlighted project in ptui's navigator, which the previous change had missed — the key was registered at session level but absent from =PROJECT_MENU_ACTIONS=, so it silently did nothing on the flat project list.
** Claude [2026-09-01 Tue 00:51 PDT]: reworked the =cdproj= load summary in =misc/cdproj.func.sh= so it reports what arrives as well as what leaves, aligns both lists under one label column, and prints nothing at all when the stack is unchanged, replacing the always-printed =dropped:= header and its =(none)=; synced the copy quoted in =docs/cdproj.md= and covered the output with a shell test.
** Gemini [2026-09-01 Tue 17:48 PDT]: filed task =t0047= to allow Left arrow on a collapsed top-level task in =orti= to return to the parent view / =ptui= project navigator via =session.pop_view()=, providing two-way directional symmetry with Right arrow tree navigation.
